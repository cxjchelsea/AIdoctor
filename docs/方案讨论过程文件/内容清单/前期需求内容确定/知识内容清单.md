下⾯这份是“按你们现有 **5** 阶段流程”，逐阶段说明 每⼀个主诉**/**症状要补⻬哪些内容，才能保证“任何症状都能按

同⼀结构推进，并最终收敛到诊断结论（或可执⾏的不确证结论）”。

我只写 阶段所需内容清单，你们后续只要对每个症状把这些内容填⻬，就能跑通全流程。

触发诊断（进⼊诊断闭环前的门控）

你们已做急危重分诊，这⾥只写“每个主诉需要准备什么内容”，保证门控可⽤

每个主诉需要补⻬的内容

1. 主诉级红旗条件（⾼危触发项）

该主诉下“出现即升级/急诊”的症状组合

例如：意识改变、⽣命体征异常、进⾏性神经缺损等（主诉特异）

2. 升级动作

命中红旗后：⽴即就医/急诊/120/线下科室建议

未命中：进⼊阶段1

阶段**1**：主动询问（症状特征｜伴随症状｜关键背景）

⽬标是把⽤户⾃然语⾔“翻译”为可推理的结构化问题清单

每个主诉需要补⻬的内容（必须）

**A.** 归⼀化词表（⼊⼝标准化）

⽤户⼝语表达 → 标准概念（同义映射）

歧义表达 → 必须追问的判定规则

（例如“胸闷”可能是呼吸困难/⼼悸/压迫感，需要澄清）

**B.** 症状特征的**“**最⼩关键字段模板**”**

这⼀套字段对所有主诉通⽤，但每个主诉要明确“哪些是必填”

起病⽅式：突然/渐进

病程：急/亚急/慢

持续模式：持续/间歇/反复

严重程度：对睡眠、活动、功能影响

部位/范围/放射（适⽤时）

性质：压榨痛/刺痛/烧灼等（主诉特异选项）

Printed using ChatGPT to PDF, powered by PDFCrowd HTML to PDF API. 

51/56诱因/缓解：运动/体位/进⾷/情绪/⽤药后变化

**C.** 伴随症状清单（主诉特异）

与该主诉“最能改变⽅向”的伴随症状（结构化选项）

例如“胸痛”伴随：出汗、恶⼼、呼吸困难、咳⾎等

**D.** 关键背景字段（主诉特异 **+** 全局通⽤）

年龄/性别（强制）

既往史：与该主诉强相关的重⼤病史

⽤药/过敏：会改变表现或⻛险者

暴露史：旅⾏、感染接触、⼿术、久坐久卧、外伤等

家族史：与该主诉强相关者

阶段**1**的标准产出（所有主诉⼀致）

结构化问题清单（**Problem List**）

信息缺⼝列表（初版）

阶段**2**：信息补全（缺⼝分级｜智能追问｜冲突检测）

⽬标是把信息补⻬到“能进⼊鉴别诊断推理”的最低门槛

每个主诉需要补⻬的内容（必须）

**A.** 缺⼝分级规则（主诉级）

把缺失信息划分为三类，并明确后果

必填缺⼝：不补⻬不能进⼊阶段3

（因为会导致“必须排除项”⽆法判断）

重要缺⼝：可进⼊但必须提⽰不确定性与⻛险

可选缺⼝：后续补充即可

**B.** 智能追问题库（按缺⼝映射）

每个必填/重要缺⼝都要有：

标准问法（统⼀表达）

答案结构（单选/多选/数值）

不确定处理（不知道/记不清/拒答时怎么记录）

**C.** 数据冲突检测规则（全局通⽤ **+** 主诉补充）

Printed using ChatGPT to PDF, powered by PDFCrowd HTML to PDF API. 

52/56时间轴冲突（“昨天开始” vs “⼀个⽉了”）

否定冲突（“不发烧”但⼜说“寒战发热”）

逻辑冲突（症状组合互相排斥）

并规定处理⽅式：复问/标不确定/暂不采⽤

阶段**2**的标准产出

缺⼝补全状态（**Ready / Not Ready**）

冲突项记录

更新后的问题清单（**Problem List v2**）

阶段**3**：可能性分析（关键分岔点｜三层分层｜多维组织）

⽬标是⽣成“候选集并分层”，给后续分流和验证提供结构⾻架

每个主诉需要补⻬的内容（必须）

**A.** 主诉鉴别诊断全集（**DDx Universe**）

要求覆盖三类：

常⻅原因

易混淆原因

严重但不能漏诊原因（must-not-miss）

**B.** 三层分层规则 **+** 默认三层候选集

对每个主诉都要能输出：

⾸要假设（1–2）

主要备选（2–5）

必须排除（1–3）

并明确每个候选的：

⼊选依据（来⾃问题清单哪些线索）

当前缺⼝（推进/排除所需的关键缺失）

**C.** 关键分岔点（**Pivotal Points**）清单（主诉级核⼼）

这是保证“任何主诉都能分流推进”的关键件

形式为“互斥对”，例如：

急性 vs 慢性

单侧 vs 双侧

Printed using ChatGPT to PDF, powered by PDFCrowd HTML to PDF API. 

53/56活动诱发 vs 静息也发⽣

发热 vs ⽆发热

进⾏性加重 vs 稳定/波动

并为每个分岔点提供：

标准问法

判定规则

命中后推动哪些⼦组上移/下移

阶段**3**的标准产出

三层候选集（带⼊选依据**+**缺⼝）

关键分岔点⾻架（后续分流路径的**“**树⼲**”**）

阶段**4**：鉴别诊断（分流路径｜证据推理｜排序固化｜验证计划）

⽬标是把“候选⽅向”推进到“可确认/可排除/需升级”，并形成可执⾏验证策略

每个主诉需要补⻬的内容（必须）

**A.** 分流路径模板（**Routing Path**）

必须能落到“可执⾏的分叉问诊”

第⼀层分叉问题（最能快速区分⼦组的 1–2 个问题）

⾼危触发条件（命中即升级或优先排除）

继续收缩路径（没命中⾼危时怎么推进）

**B.** 关键临床线索表（**Clinical Clues Table**）

对每个候选诊断（⾄少三层候选⾥的那些）补⻬：

强⽀持线索（出现则⼤幅上移）

强反对线索（出现则⼤幅下移）

需要补证据项（缺失则保持不确定）

**C.** 鉴别诊断差异点表（**Diff Table**：**A vs B**）

⽤于“最快分清两个⽅向”

最能区分的 3–5 个差异点

对应问法/体征/测量/检查项

**D.** 证据条⽬库（**Evidence Items**）

Printed using ChatGPT to PDF, powered by PDFCrowd HTML to PDF API. 

54/56把证据做成可复⽤条⽬，每条包含：

名称（症状/体征/测量/检查）

获取⽅式（追问/观察/设备/线下）

证据⽅向：⽀持/不⽀持/不确定

影响对象：推动哪个⽅向上移/下移

**E.** 验证计划库（必须**“**能改变排序**”**）

对“最可能”和“必须排除”⾄少各给⼀条验证计划：

查什么/问什么/测什么

什么结果算⽀持、什么算不⽀持

回填后如何更新三层排序

阶段**4**的标准产出

分流路径（可执⾏）

证据清单（⽀持**/**反对**/**不确定）

三层排序固化（最可能**/**必须排除**/**积极备选）

验证计划（查什么、怎么判、如何回填）

阶段**5**：结果输出（回填重排｜终点结论包｜随访升级）

⽬标是输出可⾏动、可审计的结论，并能在新信息出现时迭代

每个主诉需要补⻬的内容（必须）

**A.** 回填与重排规则（**Re-rank Rule**）

新信息出现后：哪些⽅向上移/下移/剔除/保留

证据冲突时：标不确定并回退到哪个阶段补证据

**B.** 终点结论包模板（两套：可确证 **/** 不可确证）

必须固定四要素，所有主诉⼀致：

1. 结论

可确证：输出具体诊断

不可确证：输出最可能⽅向 + 下⼀步验证

2. 必须排除项状态

已排除 / 未排除 / 需线下排除（必须写清）

Printed using ChatGPT to PDF, powered by PDFCrowd HTML to PDF API. 

55/563. 关键证据链

⾄少 3 条：阳性证据 + 关键阴性证据 + 不确定证据

4. ⾏动与随访

⽴即⾏动（居家/复查/就医）

复评时间窗（多久必须再评估）

升级触发条件（出现什么必须升级）

**C.** 回退迭代条件（避免**“**结论锁死**”**）

每个主诉都要定义：

症状恶化/出现新⾼危 → 回到触发诊断或阶段1

证据冲突/信息不⾜ → 回到阶段2

处理⽆效/病程演变 → 回到阶段3–4重建分流与验证

阶段**5**的标准产出

回填后的最新排序

终点结论包（可⾏动）

随访与升级规则（可复评、可迭代）

⼀句话总结：让**“**任何症状都能填进去跑通**”**的关键是什么？

不是增加更多阶段，⽽是保证 每个主诉在每个阶段都有同样的**“**必填内容**”**：

阶段1：能标准化表达（词表**+**关键字段）

阶段2：能补⻬到可推理（缺⼝分级**+**追问库）

阶段3：能⽣成并组织候选（**DDx**全集**+**三层分层**+**关键分岔点）

阶段4：能⽤证据推进与验证（线索表**+**差异点表**+**分流路径**+**验证计划）

阶段5：能回填收敛并可迭代（重排规则**+**终点结论包**+**随访升级）

只要每个症状把这五段内容补⻬，它就⼀定能沿同⼀条流⽔线，从症状⾛到“可确证诊断”或“可执⾏的不确证结

论”。

如果你下⼀步希望我帮你们把某个主诉（⽐如“腰痛/胸闷/腹痛”）按上述五段内容完整填⼀遍，我可以直接给出

⼀份标准样板，作为团队后续批量复制的模板。

Printed using ChatGPT to PDF, powered by PDFCrowd HTML to PDF API. 

56/56