# AIdoctor 可执行实施路线

> 文档状态：Draft v2.6 Executable Roadmap  
> 更新时间：2026-07-29  
> 目标：覆盖代码改造、旧设计资产继承、Capability、Model Runtime、RAG、数据迁移、前端、工程、评估、发布和旧系统下线。

---

## 1. 路线定位

```text
总体架构
定义最终系统是什么

Architecture Freeze Baseline
定义哪些决策稳定、如何变更

Legacy Design Asset Mapping
定义旧设计中哪些资产保留、改造、评估或归档

Implementation Roadmap
定义按什么顺序做、交付什么、如何验收

Migration Matrix
定义旧代码、数据、设计资产和基础设施如何处理

Coverage Matrix
检查目标能力和旧资产是否遗漏
```

本路线可以更新任务状态和实现细节，但不得绕过冻结的状态所有权、安全骨架、统一 Model Runtime、Knowledge Release、旧资产验证和核心依赖方向。

## 2. 执行原则

1. 按纵向闭环推进，不按十一个模块分别大而全开发；
2. 每个 Phase 必须有可运行场景、可验证资产和 Exit Gate；
3. 先稳定 Contracts、Capability、状态所有权和旧资产映射，再引入 LangGraph；
4. 重构采用资产继承式迁移，不采用默认推倒重写；
5. 保留旧 Workflow，通过 Adapter、双跑和流量切换迁移；
6. 每阶段同时覆盖后端、前端、数据、测试、可观测性和发布；
7. LLM 只通过 Model Runtime 调用；
8. 每个临床场景以 Capability Package 发布；
9. 红旗和分诊不依赖 LLM 单点执行；
10. 每个数据库变化都有 Migration、Reconciliation 和 Rollback；
11. 旧服务和旧资产只有满足 Decommission Gate 后才能归档或删除；
12. 旧文档中的“已完成”不能替代代码、测试、运行和数据证据。

## 3. 目标纵向切片

```text
成人患者：“咳嗽三天，有点喘”
→ 创建 Encounter / Thread
→ 加载 adult_respiratory_v1
→ Observation Extraction
→ State Committer
→ Mandatory Safety
→ InformationGap / QuestionDecision
→ Question Wording
→ Checkpoint / Interrupt
→ 服务重启
→ Resume
→ 更新 Observation / Triage
→ Phase D 增加有限 Hypothesis 和白名单 Evidence
→ Phase E 增加医生审核和 Delivery
```

该切片贯穿 Phase B-E，每个阶段都在前一阶段闭环基础上增加能力。

---

# Phase A：真实基线、资产继承、Contracts、Capability 与架构冻结

## A0 目标

- 证明当前项目真实能否编译、启动和运行；
- 完成代码、数据、Prompt、规则、知识、接口和原设计资产 Inventory；
- 形成 Shared Contracts v1；
- 建立 `adult_respiratory_v1` 和 Model Runtime 配置骨架；
- 完成关键 ADR、CI 和固定 Workflow 基线；
- 将 Freeze Candidate 升级为 Frozen Baseline。

## A1 Java 基线

任务：

- `diagnosis-service` Maven compile/test；
- `examination-service` Maven compile/test；
- 盘点 JDK、Spring Boot、Oracle/MySQL、Flyway、Redis、Feign 和 Nacos 依赖；
- 识别所有 CDP 创建、读取和写入点；
- 识别固定 Workflow 入口、fallback 和默认继续行为；
- 识别 AOP、`@TraceExecution`、`ExecutionTraceAspect`、`TraceContext` 和 Feign Trace 拦截器；
- 识别 Java 内 Prompt、模型 SDK 或远程模型调用；
- 输出 API、Entity、Repository、Client、Migration 和测试清单。

交付：

```text
java-build-baseline.md
java-api-inventory.csv
java-state-write-inventory.csv
java-observability-inventory.csv
```

## A2 Python 基线

任务：

- 为每个 Python 服务建立可复现依赖环境；
- 统一记录 Python、FastAPI、Pydantic、LangChain/相关库和 LLM SDK 版本；
- 启动 Parsing、Dialog、Safety、Diagnosis Engine、OCR 和 Trace 候选服务；
- 盘点所有模型直接调用点、Prompt 文件、字符串和环境变量；
- 盘点 ToolContext/ToolResult 变体、错误处理和 fallback；
- 盘点 Redis、Neo4j、Milvus 和外部知识依赖；
- 识别服务直接读取或写入 CDP 的位置；
- 识别可提取的术语、规则、问题模板、候选疾病和评估样例。

交付：

```text
python-runtime-baseline.md
python-service-inventory.csv
prompt-inventory.csv
model-call-inventory.csv
python-contract-variant-inventory.csv
```

## A3 Frontend 与 Docker 基线

任务：

- Frontend build/lint/typecheck；
- 增加最小 test runner 决策；
- 运行现有患者流程；
- 盘点 WebSocket/STOMP/API 状态；
- 盘点 ReactFlow、时间线和调用图资产；
- 校验 Docker Compose 服务缺失、端口和环境变量不一致；
- 形成当前部署拓扑和最小本地启动说明。

交付：

```text
frontend-build-baseline.md
frontend-asset-inventory.csv
deployment-topology-current.md
compose-gap-report.md
```

## A4 数据资产盘点

任务：

- 导出现有数据库表、索引和 Migration；
- 抽样 CDP CLOB/JSON 字段；
- 统计隐藏字段、空字段、非法 JSON、版本分布和未映射字段；
- 盘点 Redis Key、TTL 和持久化用途；
- 盘点 Neo4j Schema、节点、关系、来源和使用方；
- 盘点 Milvus 是否真实使用、Collection 和 Embedding 版本；
- 识别 PHI、Consent、Tenant、Retention 和审计缺口；
- 保留旧原始数据的 blob、checksum、schema 和 source record ID。

交付：

```text
database-inventory.csv
cdp-field-profile.csv
redis-inventory.csv
neo4j-inventory.csv
vector-index-inventory.csv
data-risk-report.md
```

## A5 Shared Contracts v1

首批必须落地为 JSON Schema：

```text
Encounter
EncounterCDP
ClinicalObservation
ObservationCandidate
SourceArtifact
StatePatch
CommitResult
TriageAssessment
InformationGap
QuestionDecision
CapabilityManifest
ContextEnvelope
ToolExecutionRequest
ToolResult
ModelInvocationRequest
ModelInvocationResult
CheckpointMetadata
InterruptRecord
ResumeRequest
AgentEvent
ClinicalDecisionRecord
DeliveryPackage
```

任务：

- JSON Schema 作为源；
- 生成 Pydantic、Java DTO 和 TypeScript 类型；
- 定义 Schema Version 和 backward compatibility policy；
- 建立 contract fixtures；
- 建立 Java/Python/TypeScript contract tests；
- 从旧 ToolContext/ToolResult、CDP 和 AgentState 中提取可继承字段；
- `suggested_writes` 演化为 `ProposedStatePatch`，最终写入仍由 State Committer 控制。

## A6 Capability Package 骨架

建立：

```text
capabilities/adult_respiratory_v1/
```

至少创建：

- manifest；
- population/scope；
- terminology；
- observation profile；
- safety；
- question policy；
- hypothesis；
- knowledge policy；
- runtime allowlist；
- delivery policy；
- eval fixtures。

A 阶段只要求骨架、Schema、来源标记和临床待评审内容，不要求全部临床规则完成。

## A6.5 Legacy Design Asset Validation

该工作包必须在 A7 前完成，执行依据为：

- `legacy-design-asset-retention-and-mapping.md`；
- `docs/AI医生/项目文档`；
- 对应代码、配置、数据和测试。

任务：

1. 扫描旧设计中的架构原则、协议、状态模型、流程、规则、AOP、评估和知识治理；
2. 将每项设计映射到实际代码、配置、数据和测试；
3. 登记 `DOCUMENTED / CODE_CONFIRMED / RUNTIME_VERIFIED / DATA_VERIFIED / TEST_VERIFIED`；
4. 确认 `KEEP / ADAPT / EXTRACT / SPLIT / EVALUATE / ARCHIVE / REMOVE`；对 Legacy Clinical Assets 按 [non-adoption strategy](./plans/phase-a/a6-5/a6-5-legacy-clinical-non-adoption-strategy.md) 使用 `REJECT_FOR_NEW_RUNTIME` / `KEEP_READ_ONLY` / `QUARANTINE` / `ARCHIVE`，禁止 ADOPT/ADAPT/MAP 作为新临床权威；
5. 绑定 Owner Module、Target Contract、Phase、Owner、Reviewer 和下线条件；
6. 提取双通道推理、Tool Contract、CDP Copy-on-Write、AgentState、AuditTrail、五步流程和三类评估集；
7. 专项验证 AOP/Trace：同步失败是否阻塞业务、异步上下文传播、PHI、Feign Trace 和 OTel 迁移；
8. 专项验证 Knowledge Publish Gate 和 Neo4j/DR.KNOWS 资产；
9. 回写 Inventory、Migration Matrix、Coverage Matrix 和 Decommission Register；
10. 未验证资产不得被删除或进入生产 Release。

交付：

```text
legacy-design-asset-inventory.csv
legacy-design-code-evidence.csv
legacy-contract-extraction.md
legacy-eval-asset-inventory.csv
legacy-observability-migration.md
legacy-asset-decommission-register.csv
a6-5-legacy-clinical-non-adoption-strategy.md
```

`legacy-clinical-policy-extraction.md` 不再作为 A6.5 必交付物（`REPLACE_WITH_NON_ADOPTION_EVIDENCE`；B04 提取目的已由 Owner non-adoption 决策 supersede，不得写成 EXECUTED_COMPLETE）。

Exit Gate：

- 所有 P0/P1 旧设计资产有唯一记录（KEEP）；
- 非临床 `KEEP/ADAPT` 有代码和测试验证计划（KEEP）；临床权威侧改为 non-adoption evidence（REPLACE_WITH_NON_ADOPTION_EVIDENCE）；
- `EVALUATE` 有对照实验（非临床/研究资产 KEEP）；
- `ARCHIVE/REMOVE` 有依赖、数据和回滚检查（KEEP；禁止立即删除）；
- AOP/Trace、CDP、AgentState、AuditTrail 和 Tool Contract 已形成目标映射（KEEP）；
- Coverage Matrix 无“有价值但无归属”的资产（REPLACE：临床资产归属为 REJECT/NOT_MIGRATED/FUTURE_REBUILD_REQUIRED）；
- A6.5 Exit PASS 只表示 legacy governance closed，不表示 new clinical baseline / A6 approved / A7-CL ready。

## A7 Model Runtime 骨架

- ModelSpec；
- ModelRoutePolicy；
- PromptSpec；
- Prompt Registry/Loader/Builder；
- Output Schema Registry；
- Model Gateway；
- ProviderAdapter 接口；
- `common/aidoctor_llm` Legacy Adapter；
- Prompt YAML 规范；
- 禁止新代码直接调用 Provider SDK 的 lint/architecture rule；
- 首批 route：Observation Extraction、Question Wording。

### Controlled Parallel Execution Amendment（A7-NC）

历史正常路径 `A6 → A6.5 → A7` 保持不变。由于 TASK-B04 的外部临床治理依赖仍阻塞，允许通过正式 [A7-NC Roadmap Amendment](./plans/phase-a/a7-non-clinical-roadmap-amendment.md) 建立受控的非临床平台并行 lane。该变化只属于 `IMPLEMENTATION_ORDER_AMENDMENT`，不修改冻结架构，也不表示 A6.5 已完成或 A7 已整体启动。

```text
Legacy Governance Track
Owner Non-Adoption Decision → E01 → E02 → A6.5 Exit

Non-Clinical Platform Lane
A7-NC Amendment
→ separately authorized A7-NC implementation
→ A7-NC Exit

Future New Clinical Track
new clinical governance (NOT_YET_AUTHORIZED)
→ A7-CL → Full A7 Exit
```

历史 `B04 → C02` 迁移链由 `A6.5-NONADOPT-001` supersede（B04 = NOT_REQUIRED_FOR_NEW_RUNTIME_MIGRATION；C02 = NOT_REQUIRED_FOR_REJECTED_ASSETS；均非 EXECUTED_COMPLETE）。PR #29 正文与 P7 evidence 不改写。Full A7 Rejoin 不再要求 B04/C02 COMPLETE，改为要求 A6.5 Non-Adoption Exit evidence + A7-NC Exit PASSED + 独立的新临床治理证据。

A7-NC 仅覆盖 ModelSpec、ModelRoutePolicy、PromptSpec、Registry/Loader/Builder mechanics、Output Schema Registry、Model Gateway boundary、ProviderAdapter interface、禁用的 Legacy Adapter boundary、Prompt YAML 规范、provider-SDK architecture rule、两个默认 BLOCKED route shells，以及 deterministic fake provider 和非临床合成 fixture 的未来规划。临床 Prompt、clinical eligibility、Capability binding 和 clinical evaluation 全部归 A7-CL。机器状态仍为 `BLOCKED_BY_A6_5_CLINICAL_LANE`；A6.5 non-adoption Exit 之后的剩余阻塞为新临床内容治理，不得写成 A7-CL READY/COMPLETE。

强制边界：真实 provider `FORBIDDEN`；外部模型/API 调用为 0；Shared Contracts 和 Capability 语义 diff 为 0；患者数据、PHI、clinical gold 和 activated clinical content 为 0；State Committer、Mandatory Safety、Triage、Permission、Consent 不变。A7-NC 的详细 scope、24 项 Exit Gate、13 项 Full A7 Rejoin Gate、stop/revoke 条件和批次建议以 amendment 与 [scope register](./plans/phase-a/a7-non-clinical-scope-register.csv) 为准。

当前状态（控制真值以 [2026-08-14 post-PR40 reconciliation](./plans/phase-a/phase-a-current-state-reconciliation-2026-08-14-post-pr40.md) 为准）：PR #38 / #39 / #40 已合并；`A6.5-NONADOPT-001`、E01、E02 为 `MERGED_AND_VERIFIED`；A6.5 为 `LEGACY_GOVERNANCE_CLOSED`；A6.5 Exit 为 `PASS_LEGACY_GOVERNANCE_ONLY`（≠ A6 COMPLETE / A7-CL READY）；A7-NC 现为 `COMPLETE`，Exit `PASSED`（`A7-NC COMPLETE != A7 COMPLETE`）；A7-CL 为 `BLOCKED`；A7 overall 为 `NOT_COMPLETE`；Clinical Runtime 为 `NOT_ENABLED`；Production 为 `BLOCKED`。

### Controlled Parallel Execution Amendment（PHASE_A_NC_CLOSURE）

历史正式顺序 `A1 → A2 → A3 → A4 → A5 → A6 → A6.5 → A7 → A8 → A9 → A10 → A11` **保持不变**。由于 A6.5 / A7-CL 临床治理链仍 fail-closed，而 Frozen Baseline 仍存在可独立关闭的非临床 P1 gaps，允许通过正式 [Phase A Non-Clinical Closure Roadmap Amendment](./plans/phase-a/phase-a-non-clinical-closure-roadmap-amendment.md) 建立受控的 Phase A-wide 非临床 Freeze-prep parallel lane（`PHASE_A_NC_CLOSURE`）。

该变化只属于 `IMPLEMENTATION_ORDER_AMENDMENT`，不修改冻结架构，不表示 A6.5 / A7 已完成，不授权 A8/A9/A10/A11 实施，也不降低 A11 Gate（FB-11 / FB-20 / FB-21 保留）。

```text
Legacy Governance Track
Owner Non-Adoption Decision → E01 → E02 → A6.5 Exit

Non-Clinical Closure Lane
Roadmap Amendment
→ separately authorized NC Closure batches
→ PHASE_A_NC_CLOSURE_EXIT

Future New Clinical Track
new clinical governance (NOT_YET_AUTHORIZED)
→ A7-CL → Full A7 Exit

A11 Rejoin（eligibility only）
A6.5 Exit
+ PHASE_A_NC_CLOSURE Exit
+ Full A7 Exit
+ remaining A11 prerequisites
→ A11 eligibility
（A11 eligibility ≠ A11 PASS；仍需单独授权与 Freeze Review；FB-11 新临床基线仍在）
```

`PHASE_A_NC_CLOSURE` ≠ A7-NC：A7-NC 仅覆盖 Model Runtime；本 lane 覆盖 Freeze 前非临床 closure（ADR / bindings / CI MVP / Workflow-Trace / runtime accounting / E2E design / coverage reconciliation 等）。详细 scope、exclusions、batch graph、stop/revoke 与 Exit 条件以 amendment 与 [NC Closure scope register](./plans/phase-a/phase-a-non-clinical-closure-scope-register.csv) 为准。

强制边界：真实 provider `FORBIDDEN`；PHI / clinical activation / Future Extension implementation = 0；Shared Contracts v1 语义在 lane 内不可变；Capability lifecycle 不升级；Model Runtime 语义不因本 amendment 重新打开。

当前状态：`PHASE_A_NC_CLOSURE_ROADMAP_AMENDMENT` 为 `MERGED_AND_VERIFIED`（随 PR #38）；PR #43 / `NC-CLOSE-01A` ADR decisions 现为 **`MERGED_AND_VERIFIED`**（见 [NC-CLOSE-01A Runtime & State Boundaries](./plans/phase-a/nc-close-01a/nc-close-01a-runtime-state-boundaries.md)）。`NC-CLOSE-01A` implementation 仍为 `NOT_AUTHORIZED`。PR #50 / `NC-CLOSE-01B` ADR decisions 现为 **`MERGED_AND_VERIFIED`**（见 [NC-CLOSE-01B Data & Retrieval Boundaries](./plans/phase-a/nc-close-01b/nc-close-01b-data-retrieval-boundaries.md)）。`NC-CLOSE-01B` implementation 仍为 `NOT_AUTHORIZED`。`NC-CLOSE-01` ADR decision coverage 现为 **`COMPLETE`**；umbrella implementation 仍为 `NOT_AUTHORIZED`。PR #44 / `NC-CLOSE-02` Shared Contract Bindings 现为 **`MERGED_AND_VERIFIED`**（见 [bindings README](../../contracts/v1/bindings/README.md)）。PR #45 / `NC-CLOSE-03` CI MVP 现为 **`MERGED_AND_VERIFIED`**，只保护非临床 Freeze Candidate 工程基线。PR #47 / `NC-CLOSE-01C` ADR decisions 现为 **`MERGED_AND_VERIFIED`**（见 [NC-CLOSE-01C Platform & Observability Boundaries](./plans/phase-a/nc-close-01c/nc-close-01c-platform-observability-boundaries.md)）；`NC-CLOSE-01C` implementation 仍为 `NOT_AUTHORIZED`。PR #48 / `NC-CLOSE-04` 证据现为 **`MERGED_AND_VERIFIED`**；控制状态仍为 **`PARTIALLY_VALIDATED`**（见 [NC-CLOSE-04 verification report](./evidence/phase-a/nc-close-04/nc-close-04-verification-report.md)）。不得写成 `NC-CLOSE-04` `VERIFIED` / `COMPLETE` / `SATISFIES_EXIT`。PR #49 / `NC-CLOSE-05` 证据现为 **`MERGED_AND_VERIFIED`**；控制状态为 **`RUNTIME_EVIDENCE_ACCOUNTING_COMPLETE`**（见 [NC-CLOSE-05 coordination report](./evidence/phase-a/nc-close-05/nc-close-05-coordination-report.md)）。`ACCOUNTING_COMPLETE != ALL_RUNTIME_PASS`。PR #51 / `NC-CLOSE-06` 现为 **`INTEGRATION_ARTIFACTS_MERGED_AND_VERIFIED`**（见 [E2E design](./plans/phase-a/nc-close-06/nc-close-06-e2e-design.md) 与 [current-state reconciliation](./evidence/phase-a/nc-close-06/nc-close-06-current-state-reconciliation.md)）。PR #52 / `NC-CLOSE-07` 现为 **`COMPLETE`**；`PHASE_A_NC_CLOSURE` 为 **`COMPLETE`**；`PHASE_A_NC_CLOSURE_EXIT` 为 **`PASS`**（见 [NC-CLOSE-07 Exit Record](./evidence/phase-a/nc-close-07/nc-close-07-exit-review-2026-08-18.md)）。A7-NC 为 **`COMPLETE`**。A7-CL 仍为 `BLOCKED`。FB-11 仍为 `UNSATISFIED`。A7 仍为 `NOT_COMPLETE`。A11 仍为 `NOT_PASSED`。本 Draft 上的 `A7-CL-01` 为 **`GOVERNANCE_AUTHORED_PENDING_REVIEW`**（见 [A7-CL governance charter](./plans/phase-a/a7-cl/a7-cl-clinical-governance-charter.md) 与 [Rejoin/Exit matrix](./plans/phase-a/a7-cl/a7-cl-rejoin-exit-matrix.csv)）。不得写成 `A7-CL STARTED` / `A7-CL-02 AUTHORIZED` / `FB-11 SATISFIED` / Full A7 Exit `PASS`。Clinical Runtime 仍为 `NOT_ENABLED`；Production 仍为 `BLOCKED`；Phase B 仍为 `NOT_AUTHORIZED`。`TEST PASS != REQUIREMENT PASS`。A8/A9/A10/A11 实施均为 `NOT_AUTHORIZED`。旧 A8「必须完成」清单是议题盘点（需 disposition），不是 concrete backend 实施硬条件；决策深度以 [ADR scope refinement](./plans/phase-a/nc-close-01-adr-scope-refinement.md) 为准。lane 范围仍以 [NC Closure scope register](./plans/phase-a/phase-a-non-clinical-closure-scope-register.csv) 为准。

## A8 ADR

必须完成：

1. 临床主库 PostgreSQL 还是阶段性 MySQL/Oracle；
2. Checkpoint 与临床库部署边界；
3. Java/Python 通信协议；
4. Python 首版单 Runtime package 模式；
5. LangGraph 使用方式；
6. pgvector 与 Milvus；
7. BM25 实现；
8. Neo4j 保留和生产启用门禁；
9. Secret Manager；
10. OTel Backend；
11. AOP 语义注解与 OTel Instrumentation 的迁移方式；
12. Legacy Trace Service 拆分与 AgentEvent Store 归属。

## A9 CI 与开发环境

Pipeline 至少包含：

- Java compile/test；
- Python install/lint/type/test；
- Frontend build/lint/type/test；
- Contract generation/diff；
- Schema compatibility；
- Secret/dependency scan；
- docs link check；
- migration dry-run；
- Prompt Schema；
- architecture rule：业务模块不得直连 Provider SDK；
- architecture rule：临床写入只能经过 State Committer。

## A10 固定 Workflow 与 AOP/Trace 基线

固定 Workflow：

- 跑通或明确无法跑通原因；
- 记录正常、服务失败和默认降级行为；
- 建立输入输出 fixture；
- 确定新旧双跑比较字段；
- 保留为 Phase C/F fallback。

AOP/Trace：

- 验证 `@TraceExecution` 和切面真实生效范围；
- 验证 Trace 客户端失败是否影响主流程；
- 验证输入输出脱敏；
- 验证 ThreadLocal 在线程池、异步和 Feign 场景的传播；
- 建立 OTel 对照方案；
- 区分 Technical Span、AgentEvent、ClinicalDecisionRecord 和 ComplianceAudit。

## A11 Freeze Review

进入 Frozen Baseline 前必须满足：

- [ ] Java/Python/Frontend 构建和启动基线完成；
- [ ] 数据、Prompt、Model、Knowledge、Rule、Test 和配置 Inventory 完成；
- [ ] 所有生产候选目录和服务有迁移决定；
- [ ] Legacy Design Asset Inventory 与七项交付物完成；
- [ ] AOP/Trace、CDP、AgentState、AuditTrail、Tool Contract 完成目标映射；
- [ ] Contracts v1 可生成三种语言类型；
- [ ] Capability Package 骨架完成；
- [ ] Model Runtime 接口和 Prompt 格式完成；
- [ ] 关键 ADR 批准；
- [ ] 固定 Workflow 有运行证据或明确失败原因；
- [ ] 第一条 E2E 测试设计完成；
- [ ] Coverage Matrix 无未归属能力或旧资产；
- [ ] 未验证旧能力未被删除；
- [ ] 总体状态升级为 Frozen Baseline。

---

# Phase B：Clinical State、State Committer 与呼吸道 Safety

## B0 运行场景

患者输入后，系统创建有来源的 Observation，经过唯一 State Committer 写入 CDP，并执行不可绕过的呼吸道 Safety。

## B1 新 Clinical State

- Encounter/EncounterCDP Repository；
- ClinicalObservation；
- SourceArtifact；
- Evidence Ledger；
- CDP Version、Expected Version、Snapshot；
- Patient/Tenant/Consent Scope；
- Candidate 与 Fact 分离。

## B2 State Committer

公开接口：

```python
def commit_state_patch(patch: StatePatch) -> CommitResult: ...
```

校验：Schema、Capability、字段权限、来源、Consent、expected version、conflict、operation type、Audit 和 AgentEvent。

## B3 Legacy CDP Adapter

- 读取旧 CLOB/JSON；
- 转换为新 Contract；
- 新状态映射回旧响应；
- 不允许新模块直接更新旧 Map；
- 双写仅在 ADR 批准后启用；
- 保存 migration、parse error、unmapped field 和 reconciliation 记录。

## B4 `adult_respiratory_v1` Safety Pack

- 主诉和范围识别；
- 红旗规则；
- 生命体征规则；
- 组合风险；
- 输入不足保守升级；
- 特殊人群排除；
- Emergency Guidance；
- 固定安全输出。

Safety 最终判断使用确定性规则，模型只提供 Candidate。

## B5 Clinical Parsing Adapter

- 旧 Parsing 输出适配到 ObservationCandidate；
- 保存原始 Span；
- 否定、时间、程度和不确定性；
- 概念归一化；
- 不直接写 CDP；
- 规则抽取 fallback。

## B6 Business API v2 与前端基础

- Create Encounter；
- Submit Input；
- Get Encounter State；
- v1 Compatibility Adapter；
- correlation id；
- Tenant/Consent 门禁；
- 前端使用 Encounter ID 和结构化错误模型。

## B7 Exit Gate

- State/Safety E2E 通过；
- 红旗病例集通过；
- 并发版本冲突可重现；
- 旧 CDP Adapter 对账通过；
- 临床状态无 Tool/LLM 直写路径。

---

# Phase C：Agent Runtime、Model Runtime 与 Durable Conversation

## C0 运行场景

完成一次受约束追问，保存 Checkpoint；服务重启后从 Interrupt 恢复，不重复提交状态。

## C1 Runtime

- FastAPI Agent API；
- LangGraph StateGraph；
- 显式 Mandatory Safety 节点；
- NextAction allowlist；
- Retry、Fallback 和 StopDecision；
- 旧服务通过 Legacy Adapter 接入。

## C2 Context 与 Question

- ContextEnvelope；
- Critical Context Pin；
- Token Budget；
- InformationGap；
- QuestionDecision；
- 重复问题控制；
- Prompt Wording 不新增临床事实。

## C3 Model Runtime 首批实现

- Prompt Loader/Builder；
- Model Registry/Router/Gateway；
- Provider Adapter；
- Structured Output Validator；
- Observation Extraction Route；
- Question Wording Route；
- route、prompt、model 和 capability 版本记录。

## C4 Durable Execution

- Thread/Run；
- PostgreSQL Checkpoint；
- Interrupt/Resume；
- Lease；
- Idempotency；
- Crash Matrix；
- 重复 Resume 和并发提交测试。

## C5 Observability

- OTel trace/span；
- W3C Context；
- AgentEvent；
- AOP/annotation 迁移；
- PHI-safe attributes；
- Trace 失败不阻塞业务。

## C6 Exit Gate

- 正常追问和 Resume E2E；
- 模型超时、非法 JSON 和 Provider 不可用测试通过；
- Crash Matrix 通过；
- Prompt/Model 版本可回放；
- AOP/OTel 和 AgentEvent 可关联但不混存。

---

# Phase D：有限临床推理、Patient Memory 与 Medical RAG

## D0 运行场景

在批准的呼吸道候选范围内生成有限 Hypothesis，检索白名单证据，并输出 claim-level citation 和明确不确定性。

## D1 Clinical Intelligence

- DiagnosticHypothesis；
- 支持/反对/缺失证据；
- must-not-miss；
- 信息增益和停止判断；
- 继承并改造旧五步循证流程和三层候选；
- 不输出自动确诊或治疗动作。

## D2 Patient History / Memory

- Patient RAG 独立权限域；
- 结构化查询优先；
- Longitudinal Record；
- Promotion Policy；
- Consent 撤销；
- 与 Medical RAG 零混用。

## D3 Medical RAG V1

- Source Registry；
- Source Manifest；
- Ingestion Run；
- Structure-aware Chunk；
- PostgreSQL Metadata；
- BM25；
- pgvector；
- Reranker；
- RetrievalPlan；
- EvidencePack；
- Knowledge Release。

## D4 Citation 与质量

- Claim Extraction；
- SourceSpan；
- Citation Validation；
- 适用人群、地区和时效；
- 无结果、冲突和过期表达；
- 注入攻击测试。

## D5 Knowledge Graph 对照实验

- 盘点并治理 Neo4j；
- 验证术语、Query Expansion 和 must-not-miss；
- 有图/无图对照；
- 不允许路径替代 Citation；
- 路径注入失败返回候选并进入审核，不直接形成诊断。

## D6 OCR / SourceArtifact

- OCR Adapter；
- 页面、质量和 checksum；
- 模糊/缺页/身份不符；
- 人工确认；
- 幂等上传。

## D7 Exit Gate

- RAG/Citation Eval 达标；
- Patient/Medical RAG 隔离测试通过；
- Knowledge Release 可回滚；
- Graph Benefit Report 完成；
- 旧三类评估集迁移并扩展。

---

# Phase E：医生审核、Delivery 与业务闭环

## E1 ReviewTask

- 高风险、低置信度、证据冲突和超范围触发医生审核；
- Approve/Edit/Reject；
- Clinician Resume；
- Review Decision 写入 ClinicalDecisionRecord。

## E2 三类 Delivery

- PatientDelivery；
- ClinicianDelivery；
- SystemDelivery；
- 继承旧 Conclusion Package 中的当前判断、must-exclude、关键依据、行动与随访；
- 不展示完整 Prompt、模型原始响应或私有推理过程。

## E3 Care Navigation 与 Follow-up

- 就医紧急度；
- 科室和材料建议；
- 复评窗口；
- 恶化升级条件；
- 到期、取消和失联处理。

## E4 外部动作

- Outbox/Inbox；
- 幂等；
- 模拟预约/通知；
- 权限和 Consent；
- 失败补偿。

## E5 前端闭环

- 患者端 Thread/Resume；
- 医生端 Evidence/Review；
- 管理端 Capability/Prompt/Model/Knowledge Release；
- Timeline、AgentEvent 和 Technical Trace 分视图。

## E6 Exit Gate

- 医生审核 E2E；
- Delivery 安全评估；
- 外部动作重复执行为零；
- 前端刷新恢复；
- 旧 API 兼容窗口明确。

---

# Phase F：生产治理、放量、恢复与下线

## F1 Governance 完整化

- Tool/Skill Registry；
- Prompt/Model/Capability/Knowledge Release；
- Emergency Disable；
- Reviewer 和审批；
- Release Manifest。

## F2 Durable Hardening

- Lease、Outbox/Inbox、Exactly-once effect；
- 长时间 Interrupt；
- Schema 和版本升级；
- Resume 原版本兼容；
- Replay。

## F3 Eval 与放量

```text
离线评估
→ 合成病例
→ Shadow
→ Clinician Assist
→ Restricted
→ Canary
→ 分阶段扩大
```

按 Capability Release 放量，不只按应用版本。

## F4 Backup / DR

- PostgreSQL PITR；
- Checkpoint 恢复；
- Object Storage；
- Knowledge Index 重建；
- Prompt/Model/Capability Registry；
- Neo4j；
- Audit Store；
- Secret 轮换；
- RPO/RTO 演练。

## F5 Legacy Decommission

旧服务或旧资产下线前必须：

- 新能力覆盖有效调用；
- 新旧双跑通过；
- 数据和事件迁移完成；
- Legacy Asset Inventory 有最终决定；
- 旧契约、规则、Prompt、评估集和 AOP/Trace 已迁移或归档；
- 全仓引用和生产流量为零；
- 回滚窗口结束；
- Runbook、Archive 和 Decommission Register 完成；
- Owner 批准。

删除使用独立 PR，不能与新功能开发混合。

---

## 4. 全阶段状态管理

每项任务使用：

```text
PLANNED
→ IMPLEMENTING
→ VALIDATED
→ RELEASED
→ DEPRECATED
→ DECOMMISSIONED
```

每项旧资产使用：

```text
DISCOVERED
→ EVIDENCE_LINKED
→ DECIDED
→ MIGRATING
→ SHADOW_VALIDATED
→ RETAINED / ARCHIVED / DECOMMISSIONED
```

## 5. 当前可立即执行的工作

```text
A1 Java 基线
→ A2 Python 基线
→ A3 Frontend/Docker 基线
→ A4 数据资产盘点
```

A1-A4 可以立即开始。A6.5 完成前，不允许大规模删除旧服务、覆盖旧 Prompt、清理旧规则/评估集、清空 Neo4j/向量资产，或下线 AOP/Trace 链路。
