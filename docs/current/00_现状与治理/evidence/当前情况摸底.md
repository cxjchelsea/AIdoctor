> 状态：EVIDENCE_KEEP
> 定位：Current State Assessment 过程实证稿，不是当前设计权威。
> 冻结结论见：`docs/current/00_现状与治理/Current_State_Baseline_V1.md`
> 处置决策见：`docs/current/00_现状与治理/重构决策_V1.md`
> 原工作目录：`docs/refactor2/`

---

我已经按你要求的原则开始了：**先考古、先核验 Current State，不改代码，不把历史“COMPLETE”当成事实。**

先给结论：这个仓库目前不是“旧系统”和“新系统”二选一，而是一个很典型的 **Brownfield 混合态**：

> **真实业务主链仍主要运行在旧 Java 固定 Workflow + Python 微服务体系上；与此同时，新一代 Shared Contracts、Python Runtime、Model Runtime、State Committer、Capability Package、工程治理体系已经大量进入仓库，但其中相当一部分还没有接管真实临床主链。**

而且我已经发现了几个会直接影响本轮重构判断的重要事实。

你提供的两份方法文件我已经作为本轮判断标准：前者明确要求以业务闭环、状态转换和 Eval 驱动能力建设；后者明确要求区分“代码存在 / 已实现 / 已接入 / 已验证 / 业务可用”，并允许 `UNKNOWN`。

------

# 一、先纠正一个非常重要的基线：当前应以 `main` 为代码基线

你原先指定的目标分支：

```text
agent/enterprise-agent-refactoring-plan
```

现在已经不是最新代码基线了。

2026-09-10 的 commit：

```text
617f7e45bafffdabdd16301d8e46b811006dc43a
```

明确是：

```text
Merge pull request #1 from cxjchelsea/agent/enterprise-agent-refactoring-plan

Consolidate enterprise refactoring history into main
```

也就是说，Enterprise 分支已经整体合并进 `main`。

进一步比较后：

```text
agent/enterprise-agent-refactoring-plan
ahead main: 0
behind main: 1
```

它缺的正是这个 merge commit。

因此目前最合理的 Source of Code Truth 是：

```text
Repository: cxjchelsea/AIdoctor
Current code baseline: main
HEAD: 617f7e45...
```

但这里出现了第一个文档偏差：

`docs/refactoring/README.md` 目前仍写：

```text
适用分支：agent/enterprise-agent-refactoring-plan
```

所以：

| 对象                                      | 判断                                  |
| ----------------------------------------- | ------------------------------------- |
| `main`                                    | 当前代码基线 / REAL                   |
| `agent/enterprise-agent-refactoring-plan` | 已合并历史开发分支                    |
| 重构文档中“适用分支=Enterprise”           | **STALE**                             |
| Enterprise 分支中的设计内容               | 不能因此判定过期，因为内容已进入 main |

这是本轮 Current State Baseline 的第一个修正。

------

# 二、Repository Map：当前仓库实际上有三层“年代”

从目录和代码关系来看，我建议不要把它理解成几十个平级服务，而应该先分成三个时代。

## 第一层：当前旧业务运行体系

这是目前真正能够从 Controller 追进去的运行体系：

```text
frontend
    ↓
diagnosis-service
    ↓
DiagnosisController
    ↓
DiagnosisOrchestrationService
    ↓
CDPManager
    +
DiagnosisWorkflowOrchestrator
    ↓
各 Python 微服务
```

## 第二层：历史 Agent 化尝试

在 `diagnosis-service` 中存在：

```text
agent/
├─ AgentLoop
├─ ClinicalAgentBrain
├─ ToolCaller
├─ EvidenceFusion
├─ ConflictResolution
├─ StopConditionEvaluator
├─ EscalationHandler
└─ RefusalHandler
```

这是 O→P→A→U→E 类型的 Agent Runtime 尝试，但它**不是目前 REST 诊断入口真正调用的主 Orchestrator**。

## 第三层：Enterprise 新基础设施

包括：

```text
contracts/
capabilities/
packages/model_runtime/
packages/python_runtime/
diagnosis-service/.../state/committer/
engineering/
docs/refactoring/
```

这些代表后续新的：

```text
Shared Contract
State Committer
Python Runtime
Model Runtime
Capability Package
受控 Tool Runtime
工程治理
```

但现在不能写成：

> “系统已经迁移到新 Runtime。”

目前证据不支持这个结论。

------

# 三、顶层 Repository Map

当前主要目录可以先这样定性：

| 目录                               | 层级                       | 主要职责                                                     | 当前判断                                   |
| ---------------------------------- | -------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `diagnosis-service/`               | API / Orchestrator / State | Java 主诊断服务、CDP、旧 Workflow、Agent 尝试、新 State Committer | **REAL + PARTIAL 混合**                    |
| `health-state-assessment-service/` | 业务/能力                  | 初始健康状态/入口判定                                        | **REAL/PARTIAL**                           |
| `clinical-parsing-service/`        | AI 能力                    | 临床文本解析、结构化提取                                     | **REAL/PARTIAL**                           |
| `dialog-service/`                  | AI 能力/业务混合           | 缺口识别、问题生成、对话                                     | **REAL/PARTIAL**                           |
| `diagnosis-engine-service/`        | 能力                       | DDx、推理分组等                                              | **REAL/PARTIAL**                           |
| `risk-assessment-service/`         | 能力                       | 风险与分诊                                                   | **REAL/PARTIAL**                           |
| `workup-planner-service/`          | 能力                       | 检查计划                                                     | **REAL/PARTIAL**                           |
| `treatment-engine-service/`        | 能力                       | 治疗/管理建议                                                | **REAL/PARTIAL**                           |
| `explanation-service/`             | 能力/Delivery              | 终点解释、Conclusion Package                                 | **REAL/PARTIAL**                           |
| `ocr-service/`                     | Tool                       | OCR                                                          | **REAL，且被新 Runtime 做过受控接入验证**  |
| `execution-trace-service/`         | Observability              | 旧 Trace 服务                                                | **PARTIAL / Legacy candidate**             |
| `examination-service/`             | 业务服务                   | 检查相关 Java 服务                                           | **PARTIAL / 主诊断链关系待核验**           |
| `knowledge-management-service/`    | 知识能力                   | 知识管理                                                     | **UNKNOWN / 非当前主链已确认节点**         |
| `frontend/`                        | API Consumer               | 患者/业务前端                                                | **PARTIAL，需继续追具体 API**              |
| `frontend-admin/`                  | 管理端                     | 后台管理                                                     | **PARTIAL**                                |
| `common/`                          | Shared capability          | `aidoctor_llm` 等公共能力                                    | **REAL / Legacy-to-adapt**                 |
| `contracts/`                       | Shared Contract            | 新统一契约及多语言 binding                                   | **PARTIAL / 新架构资产**                   |
| `capabilities/`                    | Capability governance      | 场景 Capability Package、schema、validator、eval             | **PARTIAL / 未临床接管**                   |
| `packages/model_runtime/`          | Runtime                    | Prompt、Provider、Gateway、Model Route                       | **PARTIAL / 新架构**                       |
| `packages/python_runtime/`         | Runtime                    | Tool Router、Executor、Checkpoint、HTTP adapter              | **PARTIAL / 非临床基础已较完整**           |
| `engineering/`                     | 工程验证                   | Docker probe、Raw OCR Runtime                                | **验证工具，不是生产 Runtime**             |
| `monitoring/`                      | Infra                      | 监控                                                         | **UNKNOWN/PARTIAL**                        |
| `science/`                         | 研究/评估                  | 科研/分析资产                                                | **非主链，待细分**                         |
| `scripts/`                         | 工程                       | 扫描、验证、运行脚本                                         | **Supporting**                             |
| `DRKnows-main/`                    | Legacy/Knowledge           | DR.KNOWS 历史资产                                            | **LEGACY / 待资产级处理**                  |
| `docs/AI医生/`                     | 历史设计                   | 原系统完整设计体系                                           | **HISTORICAL + 部分有效**                  |
| `docs/refactoring/`                | 当前重构设计/治理          | Enterprise 目标架构、迁移、Evidence、Gate                    | **当前主要设计体系，但不是 Runtime Truth** |

这里最重要的不是状态标签本身，而是：

> `diagnosis-service/` 绝对不能简单标成“旧代码”。

因为旧运行主链、新 Agent 尝试、新 State Committer 都挤在这个目录里。

------

# 四、当前已经找到真实诊断入口

这是目前最重要的考古结果。

真实 Java API 入口是：

```java
@RestController
@RequestMapping("/api/v1/diagnosis")
public class DiagnosisController
```

它注入的不是：

```text
AgentLoop
PythonRuntime
StateCommitter
```

而是：

```java
DiagnosisOrchestrationService
```

真实接口包括：

```text
POST /api/v1/diagnosis/start
POST /api/v1/diagnosis/continue
GET  /api/v1/diagnosis/{cdpId}/status
GET  /api/v1/diagnosis/{cdpId}/result
POST /api/v1/diagnosis/{diagnosisId}/answer   ← 旧接口兼容
POST /api/v1/diagnosis/{cdpId}/wellness-screening/start
```

Controller 所有核心操作都进入 `DiagnosisOrchestrationService`。

因此第一版真实 Runtime Flow 已经可以写成：

```text
HTTP Request
    ↓
DiagnosisController                    REAL
    ↓
DiagnosisOrchestrationService          REAL
    ↓
CDPManager                              REAL
    ↓
DiagnosisWorkflowOrchestrator           REAL
    ↓
Feign Client
    ↓
旧 Python 微服务                        REAL/PARTIAL
```

而不是：

```text
Controller
→ AgentLoop
→ Python Runtime
→ State Committer
```

后面这一条，目前不是主诊断 REST 链。

------

# 五、真实 `/start` 链路

`DiagnosisOrchestrationService.startDiagnosis()` 实际执行：

```text
生成 sessionId
↓
CDPManager.createCDP()
↓
建立 CDP
↓
HealthStateAssessmentClient.assessHealthState()
↓
写 healthStateAssessment
↓
把 userInput/basicInfo/symptoms/vitalSigns 写入 patientState
↓
CDPManager.updateCDP()
↓
根据 needsClinicalMode 决定：
    clinical_mode_collecting
或
    wellness_mode_pending
↓
返回 DiagnosisResponse
```

这说明：

### 事实 1

系统当前的核心运行状态对象依然是：

```text
CDP
```

### 事实 2

首次 `/start` 并不会立即进入完整五步诊断 Workflow。

如果进入 clinical mode，只返回：

```text
clinical_mode_collecting
currentStep = step1_identify_problem
```

等待后续 `/continue`。

### 事实 3

健康管理路径是：

```text
wellness_mode_pending
```

然后要求前端另外调用：

```text
/wellness-screening/start
```

所以这里确实已经存在一种“等待下一事件”的状态思想，只不过还没有形成统一的：

```text
WAITING_FOR_USER
WAITING_FOR_TOOL
PENDING
Checkpoint
Resume
```

运行模型。

------

# 六、真实 `/continue` 主链更加关键

当前 `/continue`：

```text
读取 CDP
↓
直接获取 patientState
↓
patientState.userAnswers[questionId] = answer
↓
CDPManager.updateCDP()
↓
判断 currentStep
↓
DiagnosisWorkflowOrchestrator.step1IdentifyProblem()
↓
解析病例
↓
计算 completeness
↓
生成追问
↓
completeness < 60%
    → 返回问题
    → clinical_mode_collecting

completeness >= 60%
    → executeRemainingSteps()
    → Step 2～5
    → completed
```

所以目前真实临床 Runtime 更准确的表述应该是：

> **基于 CDP + completeness 阈值驱动的固定五阶段 Workflow。**

不是当前设计文档所描述的新一代：

> deterministic lifecycle workflow + constrained agent next-action runtime。

这两个东西目前不能混为一谈。

------

# 七、当前真正生效的 Orchestrator 已基本确认

现在仓库里至少存在三个容易被误认为“主编排器”的对象。

| 对象                            | 实际角色                             | 当前主链                          |
| ------------------------------- | ------------------------------------ | --------------------------------- |
| `DiagnosisOrchestrationService` | API 层诊断总协调                     | **REAL**                          |
| `DiagnosisWorkflowOrchestrator` | 老五步临床流程                       | **REAL**                          |
| `AgentLoop`                     | O→P→A→U→E Agent 实验/旧架构尝试      | **BYPASSED / PARTIAL**            |
| `packages/python_runtime`       | Enterprise Python Runtime Foundation | **PARTIAL / 未接管临床主链**      |
| `StateCommitter`                | Enterprise 状态提交机械核心          | **PARTIAL / 未接管当前 CDP 写链** |

这是本次考古非常核心的结论。

------

# 八、`DiagnosisWorkflowOrchestrator` 是真实五步临床执行器

它实际注入：

```text
ClinicalParsingClient
DialogServiceClient
DiagnosisEngineClient
WorkupPlannerClient
TreatmentEngineClient
RiskAssessmentClient
ExplanationServiceClient
CDPManager
```

并执行：

```text
Step 1 识别问题
Step 2 构建 DDx
Step 3 建立分流路径
Step 4 采集证据与检查计划
Step 5 回填并生成终点结论
```

所以 README 所描述的“Java 主编排 + Python tools”的结构，并不是纯历史文档幻想，它仍然有大量真实代码对应。

但 README 也不是完整 Runtime Truth，因为仓库已经多出新的 Enterprise 层，而且部署配置与 README 并不一致。

------

# 九、当前发现的第一个高风险技术债：失败语义不安全

这里问题很明确，而且不是审美问题。

## 健康状态判定失败

当前代码：

```text
HealthStateAssessment 调用失败
↓
默认：
workMode = clinical_mode
needsClinicalMode = true
riskLevel = L4
↓
继续流程
```

也就是说：

> “无法完成入口健康/风险判断”会被转换成一个能够继续临床路径的默认低风险式状态。

## Clinical Parsing 失败

```text
失败
→ 使用原 patientState
→ 继续
```

## Dialog 失败

```text
失败
→ "请详细描述一下您的症状"
→ 继续
```

## Diagnosis Engine 失败

Step 2：

```text
失败
→ ddx = []
→ 继续
```

## Risk Assessment 失败

```text
失败
→ log error
→ 继续
```

所以这不是简单的：

```text
Failure Policy 不完善
```

而是当前实际存在：

```text
高风险能力失败
→ 默认继续业务
```

这和你提供的 SOP 中：

```text
高风险能力不可用
→ Fail Closed / Escalate
```

存在直接冲突。

我目前会把它列为：

```text
Risk ID: CSA-SAFETY-001
Severity: HIGH
Status: CONFIRMED
Decision: REFACTOR
```

但现在还不修改。

------

# 十、CDP 当前真实状态管理在哪里

目前旧主链核心是：

```text
diagnosis-service/
└─ service/cdp/
   └─ CDPManager.java
```

它是真正的 CDP 创建和更新入口之一。

创建：

```text
id
patientId
sessionId
version = 1
cdpStatus = initial
patientState
ddx
evidenceGraph
workupPlan
managementPlan
uncertainty
audit
```

更新时：

```text
SELECT FOR UPDATE
↓
保存旧版本
↓
applyUpdates()
↓
version + 1
↓
save()
```

因此旧系统已经存在一些值得保留的基础能力：

```text
悲观锁
Copy-on-Write/version history 思路
version
CDPRepository
CDPVersionService
```

所以后续我目前**不会建议把 CDP 体系整套推翻**。

但是其问题也已经明确：

```java
Map<String, Object> updates
```

可以直接更新：

```text
patientState
ddx
evidenceGraph
workupPlan
managementPlan
triage
uncertainty
cdpStatus
executionTrace
conclusionPackage
...
```

即：

> 旧 CDPManager 是一个权限很宽的通用状态写入口。

这和新架构要求的：

```text
Capability
→ StatePatch
→ Permission / Consent / Source / Version / Idempotency
→ StateCommitter
```

并不一致。

------

# 十一、新 `StateCommitter` 到底是什么状态

这一点现在可以非常明确。

文件自身已经写得非常谨慎：

> `COMMITTED` 只表示通过机械 admission gate，并推进机械 repository version；**不表示已经把操作应用到临床记录。**

同时文件明确写：

```text
No Spring
No HTTP
No persistence adapter
No clinical runtime
No production wiring
```

它实现了很有价值的新机制：

```text
Capability authorization
Consent authorization
Field permission
Source validation
Idempotency
baseVersion conflict
Atomic commit port
Audit
Commit event
```

所以：

```text
StateCommitter
代码存在        YES
机械核心实现    YES
测试资产存在    YES
真实持久化接入  NO EVIDENCE
临床主链接入    NO
生产接入        NO
```

准确状态应该是：

```text
IMPLEMENTED
+
PARTIALLY_VERIFIED
+
BYPASSED_BY_CURRENT_CLINICAL_RUNTIME
```

不能标记成 `REAL Clinical State Runtime`。

------

# 十二、`AgentLoop` 也不是当前真实主 Runtime

`AgentLoop` 确实已经实现：

```text
Observe
→ Plan
→ Act
→ Update
→ Evaluate
```

并具备：

```text
预算
ToolCaller
EvidenceFusion
ConflictResolution
StopCondition
Escalation
Refusal
AgentState
```

但里面有一个非常关键的事实：

```java
checkRedFlagPriority(...)
```

直接：

```java
throw new ClinicalSemanticBlockerException(
    "AgentLoop red-flag planning policy has no deterministic implementation"
);
```

类似地：

```text
Conflict Review
Insufficient Evidence
```

也仍是 blocker。

这说明这个 `AgentLoop` 不能被描述为“完成但未接入口”。

更准确应该是：

```text
AgentLoop
= PARTIAL implementation
+ BYPASSED by current REST clinical flow
+ clinical semantic blockers remain
```

因此后续做 Current State Matrix 时我不会把它归为 `KEEP`，目前更接近：

```text
REFACTOR / asset-level reuse
```

------

# 十三、新 Python Runtime 的真实定位

`packages/python_runtime/` 已经不是空骨架。

存在：

```text
artifacts.py
checkpoint.py
executor.py
tool_router.py
tools.py
protocol.py
model_runtime_port.py

http/
├─ app.py
├─ routes.py
├─ transport.py
├─ contract_validation.py
├─ controlled_composition.py
└─ engineering_raw_ocr.py
```

并且有不少测试：

```text
test_architecture_guards.py
test_http_adapter.py
test_model_runtime_port.py
test_raw_ocr_adapter.py
test_runtime_foundation.py
test_runtime_full_semantic_validator.py
test_sandboxed_artifact_backend.py
test_tool_input_foundation.py
...
```

所以这里不能标成 `STUB`。

更准确是：

```text
Runtime Foundation:
IMPLEMENTED / TESTED

Clinical orchestration:
NOT CONNECTED

Clinical production runtime:
NOT ENABLED
```

也就是说，这套新 Runtime 有实质工程资产，但仍然没有资格替代我们刚才追出来的真实五步链。

------

# 十四、Model Runtime 也是实质实现，而非纯文档

`packages/model_runtime/` 目前已有：

```text
api/
gateway/
prompts/
providers/
resources/
architecture/
```

典型文件包括：

```text
gateway/gateway.py
gateway/selection.py

prompts/
├─ loader.py
├─ builder.py
├─ registry.py
└─ models.py

providers/
├─ protocol.py
├─ fake.py
└─ models.py
```

以及 synthetic provider / prompt 资源与测试资产。

所以它目前的状态是：

```text
PARTIAL
```

不是：

```text
DESIGN ONLY
```

但同样不能写成：

```text
当前所有模型调用已统一进入 Model Runtime
```

这一点仍需在第二阶段做全仓 Model Call Inventory 才能确认。

------

# 十五、Capability Package 状态也比较清楚

现在存在：

```text
capabilities/
├─ adult_respiratory_v1/
├─ schemas/
├─ validator/
├─ tests/
├─ requirements-test.txt
└─ requirements-test.lock.txt
```

当前重构导航文档自己也承认：

```text
adult_respiratory_v1
DRAFT
PARTIALLY_VALIDATED
临床内容 REQUIRES_CLINICAL_REVIEW
runtime 未接入
production BLOCKED
```

这一部分文档和目前代码证据是基本一致的。

因此：

```text
Capability Package infrastructure = PARTIAL / valid asset
adult_respiratory clinical capability = NOT BUSINESS READY
```

------

# 十六、Shared Contracts 属于目前新架构中比较扎实的资产

存在：

```text
contracts/
├─ v1/
├─ releases/
└─ README.md
```

而 CI 中已经明确包含：

```text
JSON/schema validation
Python binding tests
Java binding tests
TypeScript binding tests
version drift check
release 1.1.0 exact validation
```

因此 Shared Contracts 比很多其他 Enterprise 模块的验证等级更高。

但仍然有一点不能跳跃：

```text
Contracts PASS
≠ current clinical runtime uses contracts everywhere
```

所以暂定：

```text
contracts/
KEEP
```

但：

```text
legacy runtime → contracts migration
PARTIAL
```

------

# 十七、CI 有一个刚出现的现实问题

这是合并 main 后形成的新问题。

现在唯一 workflow：

```text
.github/workflows/ci.yml
```

而触发条件仍然是：

```yaml
pull_request:
  branches:
    - agent/enterprise-agent-refactoring-plan

push:
  branches:
    - agent/enterprise-agent-refactoring-plan
```

但是现在 Enterprise 分支已经并入 `main`。

因此：

> **main 已经成为代码基线，但 CI 仍只监听已经成为历史开发线的 Enterprise 分支。**

更直接的证据是：main 当前 merge commit `617f7e45...` 没有关联到新的 PR-triggered workflow run。

所以这应进入 Technical Debt：

```text
CSA-CI-001
main branch is not covered by current CI trigger
CONFIRMED
```

注意，这不等于“CI 以前没跑过”。

而是：

> **当前 main 代码基线没有形成持续 CI 门禁。**

------

# 十八、README 与部署配置存在明显偏差

README 描述的是：

```text
diagnosis-service
+
tool_0 health-state
tool_1 clinical-parsing
tool_2 dialog
tool_3 diagnosis-engine
tool_4 workup
tool_5 treatment
tool_6 risk
tool_7 explanation
```

但是 `docker-compose.yml` 实际只编排：

```text
diagnosis-service
examination-service
diagnosis-engine
workup-planner
treatment-engine
risk-assessment
ocr

mysql
redis
neo4j
nacos
```

没有：

```text
health-state-assessment
clinical-parsing
dialog
explanation
execution-trace
knowledge-management
```

而我们已经从真实 Java 主链确认：

```text
HealthStateAssessmentClient
ClinicalParsingClient
DialogServiceClient
ExplanationServiceClient
```

都是当前代码明确依赖。

所以：

```text
docker-compose.yml
```

不能被视为完整可运行系统定义。

状态应是：

```text
PARTIAL
```

而不是 `REAL complete deployment topology`。

这也是后续“真实系统是否可以一键跑通”的主要 UNKNOWN 之一。

------

# 十九、旧项目文档到底是什么地位

旧设计文档体系其实非常大。

`docs/AI医生/项目文档/` 里面至少形成了完整分类：

```text
0 文档索引
1 系统概述
2 架构设计
3 业务功能设计
4 工具设计
...
10 实现与部署
11 错误处理
12 性能与评估
13 技术细节
```

具体包括：

```text
整体架构概述
主Agent架构设计
双通道推理架构
工具系统架构

临床诊疗态流程设计
健康管理态流程设计
入口判定流程设计
终点结论包设计

错误处理策略
异常处理
评估验证体系
AOP
DR.KNOWS
LangChain
...
```

我的判断不是“这些都过期”。

更准确是：

> **这套文档描述了当前旧代码大量真实来源，因此具有很高的历史解释价值；但它不再拥有整体 Source of Truth 权限。**

它里面的一些设计已经真实落地，比如：

```text
CDP
五步 Workflow
AgentLoop
ToolContext/ToolResult
Audit
Trace
ConclusionPackage
双通道部分思想
```

另一些则：

```text
存在实现但没接入
部分实现
被 Enterprise 设计替代
或仍然 UNKNOWN
```

所以旧项目文档总体状态：

```text
HISTORICAL
+
PARTIALLY_VALID
```

不能整体标：

```text
STALE
```

------

# 二十、当前 Enterprise 文档体系的真实地位

`docs/refactoring/README.md` 自己定义了一套比较明确的优先级：

```text
架构冻结基线
→ v2.6一致性
→ 原设计资产映射
→ 总架构
→ Capability/RAG/Model...
→ Inventory
→ Migration
→ Coverage
→ Roadmap
→ 专题长文
→ Future Extension
→ 旧项目文档
```

而且它自己明确写：

```text
旧文档中的“已完成”不能代替代码、测试和运行证据
```

这与本轮 Current State Assessment 的方法是一致的。

但是这套文档还有两类问题。

第一类是**事实性过时**：

```text
适用分支仍写 Enterprise branch
```

main 已经合并。

第二类是**设计与真实 Runtime 尚未收敛**：

文档要求：

```text
不可绕过 State Committer
不可绕过 Capability/Consent/Permission
Mandatory Safety
Checkpoint
医生审核
幂等
```

但当前真实 `/continue`：

```text
DiagnosisOrchestrationService
→ patientState.put(...)
→ CDPManager.updateCDP(...)
```

根本没有经过 State Committer。

所以目标设计不能标为 `ACTIVE CURRENT RUNTIME DESIGN`。

更精确：

```text
Target Architecture Source of Truth = ACTIVE
Current Runtime Description = NOT SOURCE OF TRUTH
```

------

# 二十一、Document Truth Map 第一版

现在可以先建立这一版：

| 文档/文档域                               | 类型                     | 描述哪代系统         | 与代码关系                             | 当前状态                          | 可作为什么依据               |
| ----------------------------------------- | ------------------------ | -------------------- | -------------------------------------- | --------------------------------- | ---------------------------- |
| 根 `README.md`                            | ARCHITECTURE/PROCESS     | 原微服务系统         | 大量真实，但不完整                     | `PARTIALLY_VALID`                 | 理解旧运行架构               |
| `docker-compose.yml`                      | INFRA                    | 原运行栈             | 只覆盖部分服务                         | `PARTIALLY_VALID`                 | 部分部署事实                 |
| `docs/AI医生/项目文档/*`                  | DESIGN/ARCHITECTURE      | Legacy/上一代        | 大量有代码对应                         | `HISTORICAL + PARTIALLY_VALID`    | 解释旧资产                   |
| `docs/refactoring/README.md`              | INDEX/PLAN               | Enterprise Target    | 目标设计有效，分支信息已过时           | `PARTIALLY_VALID`                 | 重构文档导航                 |
| `总架构与模块设计.md`                     | ARCHITECTURE             | Target               | 尚未全部落地                           | `ACTIVE as target`                | Target SoT                   |
| `架构冻结基线.md`                         | ARCHITECTURE/GOVERNANCE  | Target               | 门禁设计                               | `ACTIVE`                          | 目标架构约束                 |
| `v2.6跨文档一致性补充.md`                 | DESIGN                   | Target               | 设计层                                 | `ACTIVE`                          | Target correction            |
| `当前系统资产盘点.md`                     | INVENTORY                | Current/2026-07      | 与本轮已发现事实高度吻合，但已有新变化 | `PARTIALLY_VALID`                 | 历史盘点输入，不直接继承结论 |
| `代码、数据与设计资产迁移矩阵.md`         | PLAN                     | Migration            | 决策需要重新核验                       | `PARTIALLY_VALID`                 | Candidate decision           |
| `原设计资产保留...映射.md`                | DESIGN/MIGRATION         | Legacy→Target        | 映射逻辑有价值                         | `PARTIALLY_VALID`                 | 候选迁移依据                 |
| `可执行实施路线.md`                       | PLAN                     | Target delivery      | 已经历多阶段演进                       | `PARTIALLY_VALID`                 | 历史施工路线                 |
| `capability-package规范.md`               | ARCHITECTURE             | Target               | 已有真实目录/validator                 | `ACTIVE`                          |                              |
| `prompt-and-model-runtime设计.md`         | ARCHITECTURE             | Target               | 已有实质实现                           | `ACTIVE / PARTIAL implementation` |                              |
| `agent-runtime-foundations-扩展.md`       | DESIGN                   | Target/Future        | 部分 Runtime 已实现                    | `PARTIALLY_VALID`                 |                              |
| `Durable Execution 与可观测性扩展方案.md` | DESIGN                   | Target/Future        | Checkpoint 等部分基础存在              | `PARTIALLY_VALID`                 |                              |
| Phase A/A6/A7/NC-CLOSE 文档               | PLAN/EVIDENCE/GOVERNANCE | Enterprise migration | 主要用于证明阶段门禁                   | `HISTORICAL or ACTIVE EVIDENCE`   |                              |
| `adult_respiratory_v1` Evidence           | EVAL/EVIDENCE            | Target Capability    | DRAFT，未临床接入                      | `ACTIVE but BLOCKED`              |                              |

这里尤其注意：

> `ACTIVE` 不等于“已经实现”。

我这里用的是：

```text
ACTIVE design authority
```

而不是：

```text
ACTIVE runtime
```

------

# 二十二、已有的“当前系统资产盘点”不能直接复用为本轮答案，但价值很高

`docs/refactoring/当前系统资产盘点.md` 本身其实已经意识到了很多问题，例如：

```text
Diagnosis Service 同时承担 API、状态、固定流程
远程服务失败可能默认继续
Map<String,Object>
CDP 混入多类状态
多个 Python 服务职责重叠
Compose/配置必须重新核验
Prompt、Model、Knowledge 都需重新 Inventory
```

这些与我这次从当前 `main` 代码反向追踪得到的事实基本吻合。

所以它不是废文档。

但它是：

```text
2026-07-30 Draft v2.6 Inventory
```

之后仓库又经历 A6.5、A7-NC、NC-CLOSE、State Committer、Python Runtime、工程冻结以及最终 merge main。

因此它现在应该分类为：

```text
PARTIALLY_VALID
```

而不是继续作为 Current State Baseline。

本轮需要重新生成 Current State Baseline。

------

# 二十三、Code–Document Gap Map：第一版已经能确认这些

| 设计目标            | 文档要求                                   | 当前代码                                        | Gap                           |
| ------------------- | ------------------------------------------ | ----------------------------------------------- | ----------------------------- |
| 单一新 Runtime      | constrained Agent + deterministic workflow | 仍由旧 Java Orchestrator 驱动                   | **未迁移**                    |
| State Committer     | 状态写入不可绕过                           | 旧主链直接 `CDPManager.updateCDP()`             | **严重 Gap**                  |
| Capability 权限     | capability/field/consent/source gate       | 旧主链没有该 admission chain                    | **Gap**                       |
| Mandatory Safety    | 高风险能力失败阻断/升级                    | Risk failure 可继续                             | **严重 Safety Gap**           |
| Agent 动态决策      | constrained next action                    | AgentLoop 非主链且有 semantic blocker           | **PARTIAL**                   |
| Python Runtime      | 通用 Tool Runtime                          | 实现较多，但未接管临床主线                      | **PARTIAL**                   |
| Model Runtime       | 统一模型调用                               | 新 runtime 已实现，但旧模型调用是否全部迁移未知 | **UNKNOWN/PARTIAL**           |
| Capability Package  | runtime-bound scenario package             | adult respiratory 仍未接入                      | **PARTIAL**                   |
| Shared Contracts    | 跨语言统一契约                             | 已有强测试，但 Legacy 接入不完整                | **PARTIAL**                   |
| Checkpoint/Resume   | Durable execution                          | runtime 基础有 checkpoint，但临床主链未证明     | **PARTIAL**                   |
| Human Review        | 医生审核状态                               | 当前真实诊断主链未确认                          | **UNKNOWN/MISSING candidate** |
| CI baseline         | 持续门禁                                   | CI 仍监听旧 Enterprise branch                   | **当前 main 无持续门禁**      |
| Deployment topology | 完整运行环境                               | compose 缺当前主链依赖服务                      | **PARTIAL**                   |
| Failure policy      | 分级 Retry/Repair/Fail Closed              | 大量统一 fallback/continue                      | **需重构**                    |

------

# 二十四、Key File Map 第一版

现在如果你要“找东西”，先看这些地方。

| 想找什么               | 当前最关键文件                                               |
| ---------------------- | ------------------------------------------------------------ |
| 诊断 HTTP 入口         | `diagnosis-service/.../controller/DiagnosisController.java`  |
| 当前诊断总协调         | `.../service/DiagnosisOrchestrationService.java`             |
| 当前五步临床 Workflow  | `.../service/orchestration/DiagnosisWorkflowOrchestrator.java` |
| 当前 CDP 写入          | `.../service/cdp/CDPManager.java`                            |
| CDP 数据模型           | `.../entity/CDP.java`                                        |
| CDP 版本历史           | `.../service/cdp/CDPVersionService.java`                     |
| CDP Repository         | `.../repository/CDPRepository.java`                          |
| 历史 Agent Runtime     | `.../agent/AgentLoop.java`                                   |
| Agent Tool 调用        | `.../agent/ToolCaller.java`                                  |
| AgentState             | `.../entity/AgentState.java` + `AgentStateManager`           |
| 旧 Audit               | `AuditTrail` + `AuditTrailManager`                           |
| 新状态提交核心         | `.../state/committer/StateCommitter.java`                    |
| 新状态权限端口         | `.../state/committer/ports/*`                                |
| Java→新 Python Runtime | `.../client/PythonRuntimeClient.java`                        |
| 新 Python Runtime      | `packages/python_runtime/*`                                  |
| Tool Router            | `packages/python_runtime/tool_router.py`                     |
| Python Runtime HTTP    | `packages/python_runtime/http/*`                             |
| Checkpoint 基础        | `packages/python_runtime/checkpoint.py`                      |
| Model Gateway          | `packages/model_runtime/gateway/gateway.py`                  |
| Prompt Registry/Loader | `packages/model_runtime/prompts/*`                           |
| Provider Adapter       | `packages/model_runtime/providers/*`                         |
| Shared Contract        | `contracts/v1/*`                                             |
| Capability Package     | `capabilities/adult_respiratory_v1/*`                        |
| Runtime/Contract 测试  | `packages/*/tests`, `contracts/v1/tests`                     |
| Java 新 Runtime 测试   | `diagnosis-service/src/test/.../runtime`, `state/committer`  |
| 旧 Agent 测试          | `diagnosis-service/src/test/.../agent`                       |
| CI                     | `.github/workflows/ci.yml`                                   |
| 当前目标架构入口       | `docs/refactoring/README.md`                                 |
| 旧系统设计入口         | `docs/AI医生/项目文档/0.文档索引/*`                          |

------

# 二十五、当前最大的 UNKNOWN

现在不是“系统到底有没有代码”这种 UNKNOWN 了。真正大的 UNKNOWN 已经收敛到下面这些方向：

| UNKNOWN                                                      | 为什么重要                                    |
| ------------------------------------------------------------ | --------------------------------------------- |
| **当前整套系统是否还能按真实外部依赖完整启动**               | compose 并不包含全部主链服务                  |
| **前端现在实际调用哪些 API**                                 | 决定真正用户入口是否就是我们追到的 Controller |
| **各 Python 服务内部哪些能力是真模型、规则、Mock、Fallback** | 代码存在不能代表能力可信                      |
| **当前数据库是否有真实可用 CDP 数据及 Schema 漂移**          | 决定状态迁移策略                              |
| **Neo4j / Milvus /知识库实际调用情况**                       | 不能根据配置判断                              |
| **模型调用是否还有大量绕过 Model Runtime 的路径**            | 决定 Model Runtime 实际接管程度               |
| **State Committer 是否除测试/工程路径外还有生产 adapter**    | 当前看没有，但需全仓最终核验                  |
| **AgentLoop 是否还有其他非 Controller 的真实调用入口**       | 当前主诊断链绕过它，但需排除旁路              |
| **旧 execution-trace 实际运行程度**                          | Trace ≠ durable state                         |
| **哪些旧测试仍验证当前真实业务链**                           | 现在测试数量本身没有意义                      |
| **是否存在真实业务 E2E**                                     | 这是 BUSINESS_PASS 的关键                     |
| **AI Eval 是否真正绑定 model/prompt/knowledge/tool/config 版本** | 当前仍未完成能力可信度判断                    |
| **Human-in-the-loop / WAITING / resume 是否有真实主链实现**  | 新设计里重要，当前旧链看不到完整实现          |

------

# 二十六、因此，目前“哪套才是真实生效系统”的回答

现在已经可以给一个比较明确的答案：

```text
当前真实业务系统
=
Legacy Java Diagnosis Runtime
+
CDPManager
+
DiagnosisWorkflowOrchestrator
+
旧 Python Tool Services
+
部分旧 Trace / Frontend / Data Infrastructure
```

而：

```text
AgentLoop
```

是：

```text
历史 Agent 化架构尝试
PARTIAL + BYPASSED
```

新 Enterprise：

```text
Shared Contracts
State Committer
Python Runtime
Model Runtime
Capability Package
Engineering Governance
```

是：

```text
真实存在
不少部分已经实现并验证
但只完成基础设施级/非临床级逐步建设
尚未接管真实临床主链
```

所以如果现在有人说：

> “AIdoctor 已经完成了 Enterprise Agent Runtime 重构。”

这是**错误的**。

如果说：

> “AIdoctor 仍然只是最早那套五步微服务系统，新重构只是文档。”

这也**错误**。

最准确的是：

> **仓库已经完成了相当多新一代 Runtime、Contract、State、Model、Capability 和治理基础设施建设，但临床业务主链仍运行在旧编排模型上，因此整个项目当前处于“新基础设施已经形成、业务 Runtime 尚未迁移”的过渡态。**

------

# 二十七、当前第一阶段状态

根据你提供的 Current State Assessment 方法， 我现在不会进入：

```text
目标架构再设计
第一业务闭环拆分
代码修改
```

目前第一阶段可以认为已经建立了**可信的骨架基线**：

```text
Repository Topology              已建立
Canonical Branch                 已确认：main
Main REST Entry                  已确认
Current Orchestrator             已确认
Current CDP Writer               已确认
Legacy Agent status              初步确认
State Committer status           初步确认
Python Runtime status            初步确认
Model Runtime status             初步确认
Capability Package status        初步确认
CI branch drift                  已确认
Deployment mismatch              已确认
Document generations             已分层
Major Code–Document Gaps         已建立
```

但我还**不会把“完整 Repository Archaeology”标成 PASS**。

因为剩余工作不是再读更多设计，而是进入更细的实证盘点：

```text
逐 Python service
→ 真入口
→ 真算法/模型/规则
→ 输入输出
→ Fallback
→ CDP/状态关系

逐前端
→ 实际 API 调用

逐测试
→ 证明什么
→ 是否覆盖当前主链

逐 DB / migrations / config
→ 当前 Schema

逐 Prompt / Model Call
→ 谁还绕过新 Model Runtime

逐 Knowledge/RAG
→ 真实数据与调用链

逐关键历史设计文档
→ 与代码一一对应
```

这才会把目前的 `Repository Map V0.1` 收敛成真正可以冻结的 **Current State Baseline V1**。

现阶段最重要的判断已经足够明确：**不要重新做一个系统，也不要现在去优化 RAG、AgentLoop 或 State Committer。当前首先要把“旧真实主链到底有什么、哪些新基础设施已经可以被复用、旧主链如何依赖各能力”彻底核清。** 这与你给出的两份方法文件是一致的，而且当前代码已经证明这个顺序是必要的。