# C01-U01 临床理解能力 — 实施与验证记录

> 基线：`main@31e77d686e43072096fa52ed5c891bc01c17e76a`  
> 分支：`impl/c01-u01-clinical-understanding`  
> 范围：仅 U01 首个消费者所需的 C01 最小能力切片，不扩展到 U02 Clinical Fact parsing。  
> 当前状态：`IMPLEMENTED / COMPONENT_VERIFIED / INTERNALLY_WIRED_TO_U01 / NOT_EXTERNAL_BUSINESS_WIRED`。

## 1. 权威语义

按当前 Phase 7 / Phase 8：

```text
raw user input
→ C01 candidate extraction
→ typed Capability Result
→ U01 Business Owner / D10
→ formal Subject / Problem / Scope decision
```

必须保持：

```text
Capability Result != Clinical Truth
ScopeCandidate != ScopeDecision
EarlySafetySignalCandidate != Clinical Risk
Capability failure != negative clinical result
Candidate != Decision != Proposal != Commit
```

C01-U01 只产生 Subject / Problem / Scope / early-safety 候选及 source / provenance / uncertainty / binding metadata。

## 2. 现有资产处置

| 资产 | 处置 | 说明 |
|---|---|---|
| `clinical-parsing-service` | ADAPT + REFACTOR | 复用底层词表、概念识别、歧义检测 |
| `ConceptRecognizer` | REUSE / ADAPT | 移除原始临床文本日志，避免形成 PHI 日志汇 |
| `AmbiguityDetector` | REUSE / ADAPT | 作为候选不确定性输入 |
| legacy `ClinicalParsingService` orchestration | BLOCK_NEW_C01_USE | 不作为新 C01 正式编排入口 |
| legacy `tool_1` / `suggested_writes` | LEGACY_ONLY / BLOCK_NEW_C01_USE | 不允许提升为正式 State Proposal |
| U01 `U01SemanticPolicy` | KEEP | 继续拥有正式 Subject/Problem/Scope 解释 |

## 3. 已实现内容

### 3.1 Typed C01-U01 Contract

新增 typed request/result，显式区分：

- `SubjectCandidate`
- `ProblemCandidate`
- `ScopeCandidate`
- `EarlySafetySignalCandidate`
- `EvidenceSpan`
- `businessStatus / reasonCode / retryable`
- `sourceAttribution / provenance`
- `bindingId / capabilityVersion / capabilitySetVersion / scopeVersion / contractVersion`

不包含 `suggested_writes`、`StateChangeProposal` 或 CDP 写入字段。

### 3.2 C01-U01 最小能力

新增 C01-U01 service：

- SELF / OTHER / UNKNOWN Subject 候选；
- OTHER 关系词不伪造正式 `subjectReferenceId`；
- Problem 候选与 evidence span；
- `SYMPTOM / EXAMINATION / CLINICAL_CONSULTATION / OUTSIDE_V1_INTENT / MIXED / UNKNOWN` Scope 候选；
- MIXED 不掩盖临床不适；
- early safety clue 只做候选透传；
- 空输入、未知输入、binding mismatch 使用一等 failure / insufficiency 语义；
- C01 不直接产生业务真值。

### 3.3 Governed Route / Java Gateway

新增 typed route：

`POST /api/v1/capabilities/c01/u01/interpret`

Diagnosis 侧新增 typed client / DTO / gateway：

- 默认 `aidoctor.capability.c01-u01.enabled=false`；
- 未启用时禁止调用；
- 精确验证 capability/scope/contract binding；
- dependency failure 显式映射为 `DEPENDENCY_FAILURE`，不得伪装为 UNKNOWN / negative clinical result；
- `INSUFFICIENT_INFORMATION` 是合法候选结果，不等于依赖失败。

这里的 default-off gateway 仅构成当前最小受治理调用门；不声称已经具备完整 P06 生产能力包注册/审批/回滚体系。

### 3.4 C01 → U01 内部接入

新增 `U01NaturalLanguageStartService`：

```text
raw text
→ C01 candidates
→ U01StartCommand
→ U01SemanticPolicy / D10
→ U01 Consultation state
```

保持：

- C01 inferred OTHER 不产生正式 Subject Reference；
- 已有正式 Subject Reference 只在 OTHER candidate 时透传；
- canonical replay 不重新执行 C01 candidate extraction；
- raw input 使用 source fingerprint 稳定 canonical event identity，避免未来 C01 算法变化改变同一原始输入的 replay identity。

该服务是内部业务适配，不是 Phase 10 外部 HTTP cutover。

### 3.5 PHI 日志边界

C01 不复用会记录原始临床文本的 legacy orchestration；底层 `ConceptRecognizer` 日志改为仅记录长度/计数，不写原始临床文本。

## 4. Eval / Test 覆盖

C01-U01 最小 Eval 覆盖：

- SELF 临床诉求；
- OTHER 关系主体；
- OTHER 不伪造 Subject Reference；
- mixed outside + clinical discomfort；
- outside V1 intent；
- vague / unknown input；
- empty input；
- binding mismatch fail-closed；
- typed HTTP contract 无 direct-write 字段；
- architecture guard 不回流 legacy orchestration / direct-write semantics；
- Java gateway default-off / exact binding / dependency failure；
- C01 → U01 mapping；
- canonical replay skip re-interpretation；
- Foundation / U01 / StateCommitter 回归；
- diagnosis-service 全量回归；
- legacy clinical-parsing regression。

这些测试证明的是 **U01 所需最小 deterministic/structural capability slice**，不是广义临床 NLP 准确率、临床有效性或生产安全性。

## 5. 独立实现审查与整改

### C01-R01 — 新能力回流 legacy orchestration / PHI logging 风险

初版复用了旧 parsing orchestration。整改为只复用低层 `VocabularyLoader / ConceptRecognizer / AmbiguityDetector`，并移除底层 raw clinical text logging。

状态：CLOSED。

### C01-R02 — 架构 guard 误报

guard 曾因注释文本包含 legacy 类名而失败。未削弱 guard；改为检查可执行引用，并移除误报注释来源。

状态：CLOSED。

### C01-R03 — Candidate ownership

复核确认 C01 result 无 CDP write、无 `suggested_writes`、无 K09 Proposal；正式 Subject/Problem/Scope 仍由 U01/D10 解释。

状态：CLOSED / PASS。

### C01-R04 — Capability activation / production authority

当前 gateway 为 default-off，binding 使用当前 U01 固定版本常量；尚未形成完整 P06 生产注册、审批、过期和回滚体系。

状态：NON_BLOCKING_FOR_COMPONENT_SLICE / BLOCKS_PRODUCTION_AUTHORIZATION。

### C01-R05 — Runtime ingress ordering / Trace

当前 C01→U01 是内部自然语言适配层，尚未替换外部 `/diagnosis/start`，也不声称已经完成 Phase 10 ingress、完整 P05 capability-call trace 或生产 Runtime 排序。

状态：DEFER_TO_EXTERNAL_INTEGRATION / NOT_A_COMPONENT_VERIFICATION_BLOCKER。

## 6. 最终验证证据

最终验证代码 HEAD：`cf7cafb8faa2ba86c9c81c8eb71c6fd5b01cce51`  
GitHub Actions Run：`34566341177`

结果：

- Python dependency install：PASS
- clinical-parsing-service compile：PASS
- C01-U01 Eval / Contract slice：PASS
- legacy clinical-parsing regression：PASS
- Shared Contracts Java binding install：PASS
- diagnosis-service compile：PASS
- C01 / U01 / Foundation / StateCommitter focused tests：PASS
- diagnosis-service full regression：PASS
- `c01-python`：SUCCESS
- `c01-java`：SUCCESS

## 7. 当前状态

```text
C01-U01 Capability Assessment        = COMPLETE
C01-U01 Code                         = IMPLEMENTED
C01-U01 Typed Contract               = IMPLEMENTED
C01-U01 Minimal Eval                 = PASS
C01-U01 Component Verification       = PASS
C01 → U01 Internal Candidate Wiring  = WIRED / VERIFIED
C01 Broad Clinical NLP Evaluation    = NOT_COMPLETE
P06 Production Capability Governance = NOT_COMPLETE
P05 Full Capability-call Trace       = NOT_COMPLETE
External /diagnosis/start Cutover    = NOT_COMPLETE
U01 External Business Wiring         = NOT_COMPLETE
Clinical Runtime Production          = NOT_ENABLED
Production Authorization             = BLOCKED
U02                                  = NOT_STARTED BY THIS SLICE
Legacy Removal                       = NOT_AUTHORIZED
```

## 8. 下一门禁

C01-U01 当前已达到 U01 内部能力切片的组件级门槛。后续若要把真实用户请求接入新链，应进入 Phase 10 / external integration，并补齐外部 Contract、正式 capability binding/trace 与 E2E；不得因本记录 PASS 直接删除 legacy `/diagnosis/start` 链或宣称临床 NLP 已验证。
