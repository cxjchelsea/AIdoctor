# AI医生系统 - 评估验证体系

> **文档定位**：本文档详细说明AI医生系统的评估验证体系，包括三类离线评测集、核心评估指标、工具性能评估、主Agent性能评估等。  
> **相关文档**：
> - [性能优化设计](./性能优化设计.md)：请参考《性能优化设计》了解性能优化策略
> - [复杂度分析](./复杂度分析.md)：请参考《复杂度分析》了解系统复杂度

---

## 一、评估验证体系概述

### 1.1 评估目标

**核心目标**：
1. **诊断准确性**：评估系统诊断的准确性
2. **安全性**：评估系统识别高危情况的能力
3. **问诊质量**：评估系统问诊的质量和效率
4. **可解释性**：评估系统推理过程的可解释性
5. **系统性能**：评估系统的整体性能

### 1.2 评估方法

**评估方法**：
- **离线评估**：使用静态病例集、交互问诊集、轨迹回放集进行离线评估
- **在线评估**：监控在线系统的性能指标
- **对比评估**：对比不同策略和版本的效果

---

## 二、三类离线评测集

### 2.1 静态病例集（Static Case Set）

**用途**：评估"读病历 → 给DDx/计划"的能力

**输入**：固定的完整病历文本  
**输出**：DDx列表、检查建议、处置方案  
**评估**：可重复、可对比

**评估指标**：
- DDx Top-K命中率
- 检查建议合理性
- 处置方案合理性

**工具评估重点**：
- **病例理解工具（tool_1）**：概念归一化准确率、结构化提取完整率
- **鉴别诊断工具（tool_3）**：DDx准确性、三层分层合理性（基于知识库）、路径验证有效性、路径约束推理准确性
- **检查建议工具（tool_4）**：检查建议合理性、验证计划有效性
- **治疗建议工具（tool_5）**：处置方案合理性

### 2.2 交互问诊集（Interactive Interview Set）

**用途**：评估"问对问题"的能力和问诊效率

**输入**：初始主诉，需要多轮对话  
**输出**：问诊问题序列、最终诊断  
**评估**：问诊质量、问诊效率

**评估指标**：
- "缺失信息"是否合理（问诊质量）
- 问诊轮次效率（完成诊断所需轮次）
- 信息增益有效性（问的问题是否有助于区分DDx）

**工具评估重点**：
- **主动问诊工具（tool_2）**：问诊质量、问诊效率、信息增益有效性
- **临床决策分析**：问题选择是否基于临床决策理论
- **主诉关键线索库**：线索使用是否合理

### 2.3 轨迹回放集（Trajectory Replay Set）

**用途**：评估"信息逐步揭示时修正DDx"的能力

**输入**：逐步提供信息
- 先给主诉
- 再给体征
- 再给检验
- 再给影像

**输出**：每个阶段的DDx更新  
**评估**：是否能随着信息更新修正DDx

**评估指标**：
- DDx修正准确性
- 信息更新后的诊断更新及时性
- 证据链更新完整性

**工具评估重点**：
- **鉴别诊断工具（tool_3）**：DDx修正准确性、三层排序更新及时性
- **检查建议工具（tool_4）**：证据回填准确性、排序更新规则有效性
- **证据链工具（tool_7）**：证据链更新完整性

---

## 三、核心评估指标

### 3.1 DDx准确性指标

**指标类型**：
- **DDx Top-1命中率**：最高可能性诊断是否正确
- **DDx Top-3命中率**：前3个可能性中是否包含正确诊断
- **DDx Top-5命中率**：前5个可能性中是否包含正确诊断

**工具评估**：
- **鉴别诊断工具（tool_3）**：知识库候选检索准确性、路径验证有效性、路径约束推理准确性
- **三层分层合理性**：基于知识库默认层级（Tier1/Tier2/Tier3）的划分是否合理

### 3.2 安全性指标

**指标类型**：
- **红旗识别召回率**：宁可误报，不能漏报
- **高危升级及时性**：识别高危后是否及时建议就医
- **不确定性表达准确性**：低置信度时是否明确表达不确定性

**工具评估**：
- **健康状态判定工具（tool_0）**：危险信号检查召回率
- **风险评估工具（tool_6）**：高危识别召回率、升级及时性
- **证据链工具（tool_7）**：不确定性表达准确性

### 3.3 问诊质量指标

**指标类型**：
- **信息增益有效性**：问的问题是否有助于区分DDx
- **问诊轮次效率**：完成诊断所需轮次
- **缺失信息合理性**：识别的缺失信息是否合理

**工具评估**：
- **主动问诊工具（tool_2）**：临床决策分析驱动的问诊有效性
- **主诉关键线索库**：线索使用是否合理
- **差异点词库与问法规范**：问法是否统一、答案是否可比

### 3.4 可解释性指标

**指标类型**：
- **证据链完整性**：支持证据、反对证据、缺失证据是否完整
- **推理路径可追溯性**：诊断结论是否能追溯到证据
- **方案合理性**：基于路径/证据可解释

**工具评估**：
- **证据链工具（tool_7）**：证据链构建完整性、推理路径可视化
- **鉴别诊断工具（tool_3）**：诊断树结构清晰性、推理子组组织合理性
- **所有工具**：输出是否可追溯、可审计
- **主Agent**：运行循环效率、工具调用决策准确性、证据融合有效性

### 3.5 健康状态判定指标

**指标类型**：
- **健康状态判定准确率**：是否正确判断是否需要进入诊疗流程
- **健康管理态升级及时性**：发现风险信号后是否及时升级到临床诊疗态
- **漏诊率**：应该进入临床诊疗态但误判为健康管理态的比例（必须极低）

**工具评估**：
- **健康状态判定工具（tool_0）**：入口判定流程准确性、路径选择合理性
- **风险评估工具（tool_6）**：升级触发条件准确性、升级及时性
- **主Agent**：停止条件判断准确性、升级策略执行准确性、拒答策略执行准确性

---

## 四、工具性能评估

### 4.1 评估指标

**评估指标**：

1. **任务完成率**：完成任务数 / 调用次数
2. **响应时间**：从接收调用到返回结果的时间
3. **准确率**：任务结果的准确率
4. **质量指标**：confidence、completeness、accuracy等质量指标

### 4.2 各工具评估重点

**工具评估重点**：

- **健康状态判定工具（tool_0）**：入口判定准确率、路径选择合理性
- **病例理解工具（tool_1）**：概念归一化准确率、结构化提取完整率
- **主动问诊工具（tool_2）**：问诊质量、问诊效率、信息增益有效性
- **鉴别诊断工具（tool_3）**：DDx准确性、三层分层合理性（基于知识库）、路径验证有效性、路径约束推理准确性、诊断树组织合理性
- **检查建议工具（tool_4）**：检查建议合理性、验证计划有效性
- **治疗建议工具（tool_5）**：处置方案合理性
- **风险评估工具（tool_6）**：高危识别召回率、升级及时性
- **证据链工具（tool_7）**：证据链完整性、可解释性

---

## 五、主Agent性能评估

### 5.1 评估指标

**评估指标**：

1. **运行循环效率**：完成一个运行循环的平均时间
2. **工具调用决策准确性**：工具调用决策的准确性（是否调用了正确的工具）
3. **证据融合有效性**：证据融合和冲突解决的有效性
4. **停止条件判断准确性**：停止条件判断的准确性
5. **升级策略执行准确性**：升级策略执行的准确性
6. **拒答策略执行准确性**：拒答策略执行的准确性

### 5.2 主Agent评估重点

**主Agent评估重点**：

- **运行循环效率**：Observe→Plan→Act→Update→Evaluate→Stop/Escalate循环的平均时间
- **工具调用决策**：是否在正确的时机调用了正确的工具
- **证据融合**：evidence fusion和conflict resolution的有效性
- **停止/升级/拒答决策**：停止条件、升级条件、拒答条件的判断准确性

---

## 六、主Agent决策效果评估

### 6.1 评估指标

**评估指标**：

1. **证据融合成功率**：成功融合证据的比例
2. **冲突解决时间**：解决冲突的平均时间
3. **工具调用效率**：工具调用的效率和准确性
4. **系统整体性能**：整个系统的性能指标

### 6.2 主Agent决策模式评估

**主Agent决策模式评估**：

- **工具调用决策**：工具调用决策的准确性、调用顺序的合理性
- **证据融合**：evidence fusion的有效性、融合结果的准确性
- **冲突解决**：conflict resolution的有效性、解决策略的合理性
- **停止/升级/拒答决策**：停止条件、升级条件、拒答条件判断的准确性

---

## 七、对比评估

### 7.1 对比维度

**对比维度**：

1. **单主Agent vs 多工具架构**：对比单主Agent架构和多工具架构的性能（历史对比）
2. **不同证据融合策略**：对比不同evidence fusion策略的效果
3. **不同冲突解决策略**：对比不同conflict resolution策略的效果
4. **知识库优先 vs 路径优先**：对比知识库优先策略和路径优先策略的诊断准确性
5. **不同问诊策略**：对比不同问诊策略的效率和质量
6. **不同工具调用策略**：对比不同工具调用策略的效率和准确性

---

## 八、评估体系技术实现

### 8.1 评估系统架构

**评估系统设计**：

```
┌─────────────────────────────────────────────────────────────┐
│                    评估系统架构                                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  评估数据集管理模块                                      │  │
│  │  - 静态病例集管理                                        │  │
│  │  - 交互问诊集管理                                        │  │
│  │  - 轨迹回放集管理                                        │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  评估执行引擎                                            │  │
│  │  - 自动化评估流程                                        │  │
│  │  - 指标计算引擎                                          │  │
│  │  - 结果分析引擎                                          │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  评估结果存储                                            │  │
│  │  - 评估结果数据库                                        │  │
│  │  - 评估报告生成                                          │  │
│  │  - 历史对比分析                                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 评估数据集管理

**静态病例集数据结构**：

```python
class StaticCaseSet:
    """
    静态病例集数据结构
    """
    def __init__(self):
        self.case_id: str  # 病例ID
        self.case_text: str  # 完整病历文本
        self.ground_truth: {
            "diagnosis": [  # 标准诊断列表
                {
                    "disease_code": "CUI/ICD编码",
                    "disease_name": "疾病名称",
                    "rank": 1,  # 诊断优先级
                    "is_correct": True  # 是否为正确诊断
                }
            ],
            "workup_plan": [  # 标准检查建议
                {
                    "test_name": "检查名称",
                    "purpose": "检查目的",
                    "priority": "优先级"
                }
            ],
            "management_plan": [  # 标准处置方案
                {
                    "type": "处置类型",
                    "content": "具体方案"
                }
            ]
        }
        self.metadata: {
            "source": "数据来源",
            "expert_annotation": "专家标注信息",
            "difficulty_level": "难度等级"
        }
```

### 8.3 评估执行引擎

**评估执行引擎实现**：

```python
class EvaluationEngine:
    """
    评估执行引擎
    """
    def evaluate_static_case_set(self, case_set: StaticCaseSet, system_output: dict) -> dict:
        """
        评估静态病例集
        """
        # 1. DDx准确性评估
        ddx_metrics = self.metric_calculator.calculate_ddx_accuracy(
            ground_truth=case_set.ground_truth["diagnosis"],
            system_output=system_output["ddx"]
        )
        
        # 2. 检查建议合理性评估
        workup_metrics = self.metric_calculator.calculate_workup_reasonableness(
            ground_truth=case_set.ground_truth["workup_plan"],
            system_output=system_output["workup_plan"]
        )
        
        # 3. 处置方案合理性评估
        management_metrics = self.metric_calculator.calculate_management_reasonableness(
            ground_truth=case_set.ground_truth["management_plan"],
            system_output=system_output["management_plan"]
        )
        
        return {
            "ddx_metrics": ddx_metrics,
            "workup_metrics": workup_metrics,
            "management_metrics": management_metrics,
            "overall_score": self._calculate_overall_score(
                ddx_metrics, workup_metrics, management_metrics
            )
        }
```

### 8.4 指标计算引擎

**指标计算引擎实现**：

```python
class MetricCalculator:
    """
    指标计算引擎
    """
    def calculate_ddx_accuracy(self, ground_truth: list, system_output: list) -> dict:
        """
        计算DDx准确性指标
        """
        # 1. Top-1命中率
        top1_hit = 1 if system_output[0]["disease_code"] == ground_truth[0]["disease_code"] else 0
        
        # 2. Top-3命中率
        top3_codes = [d["disease_code"] for d in system_output[:3]]
        ground_truth_codes = [d["disease_code"] for d in ground_truth]
        top3_hit = 1 if any(code in top3_codes for code in ground_truth_codes) else 0
        
        # 3. Top-5命中率
        top5_codes = [d["disease_code"] for d in system_output[:5]]
        top5_hit = 1 if any(code in top5_codes for code in ground_truth_codes) else 0
        
        return {
            "top1_hit_rate": top1_hit,
            "top3_hit_rate": top3_hit,
            "top5_hit_rate": top5_hit,
            "average_rank": self._calculate_average_rank(ground_truth, system_output)
        }
    
    def calculate_safety_metrics(self, red_flags: list, system_output: dict) -> dict:
        """
        计算安全性指标
        """
        # 1. 红旗识别召回率
        detected_red_flags = system_output.get("red_flags", [])
        true_positives = len(set(red_flags) & set(detected_red_flags))
        recall = true_positives / len(red_flags) if red_flags else 0
        
        # 2. 高危升级及时性
        upgrade_time = system_output.get("upgrade_time", 0)
        expected_time = system_output.get("expected_upgrade_time", 0)
        timeliness = 1 - min(upgrade_time / expected_time, 1) if expected_time > 0 else 0
        
        # 3. 不确定性表达准确性
        uncertainty_expression = system_output.get("uncertainty_expression", {})
        confidence = system_output.get("confidence", 1.0)
        uncertainty_accuracy = 1.0 if (confidence < 0.7 and uncertainty_expression) else 0.0
        
        return {
            "red_flag_recall": recall,
            "upgrade_timeliness": timeliness,
            "uncertainty_accuracy": uncertainty_accuracy
        }
```

### 8.5 评估结果存储与报告生成

**评估结果数据库设计**：

```sql
-- 评估结果表
CREATE TABLE evaluation_result (
    id VARCHAR(64) PRIMARY KEY,
    evaluation_type VARCHAR(32),  -- static/interactive/trajectory
    case_id VARCHAR(64),
    system_version VARCHAR(32),
    evaluation_timestamp TIMESTAMP,
    metrics JSON,  -- 评估指标结果
    overall_score DECIMAL(5,2),
    created_at TIMESTAMP,
    INDEX idx_evaluation_type (evaluation_type),
    INDEX idx_system_version (system_version)
);

-- 评估报告表
CREATE TABLE evaluation_report (
    id VARCHAR(64) PRIMARY KEY,
    report_name VARCHAR(255),
    report_type VARCHAR(32),  -- daily/weekly/monthly
    evaluation_period_start TIMESTAMP,
    evaluation_period_end TIMESTAMP,
    summary_metrics JSON,
    detailed_results JSON,
    comparison_with_previous JSON,
    created_at TIMESTAMP
);
```

---

**文档来源**：
- 原文档：《AI医生系统-技术架构设计-性能与评估.md》十三、评估与验证体系（第9-509行）
- 创建时间：2025-01-22
- 文档版本：v1.0

